# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Operating conventions for this repo

- Respond to the user in Ukrainian in this repo's sessions.
- **Keep chat replies terse — token economy matters to the account owner.** Don't narrate intermediate steps as they happen (checking CI status, marking a PR ready, merging, redeploying, removing a scratch worktree, deleting a local branch, rebasing) — just do them. No preambles ("Тепер перевірю...", "Далі зроблю..."), no step-by-step play-by-play. After the work is done, give one short final summary: what changed, the PR/commit reference if relevant, and what's next (if anything) — a few lines, not a report. This applies to conversational replies only; it does **not** relax `CHANGELOG.md` entries (Session logging convention below still wants full detail there) or PR descriptions, which stay as complete as this file's existing conventions already require.
- **Pre-launch, no real users yet** (confirmed by the account owner, 2026-07-13): the app is still in testing, not yet published to Google Play, and has no production user base. Treat backward-compatibility-with-existing-user-data as *not* a constraint until the account owner says otherwise (e.g. after the Play Store launch) — prefer clean naming/schema over legacy-compat shims.
- Standing authorization to merge PRs and deploy (Hosting/Firestore rules/Cloud Functions) autonomously, without asking for confirmation each time. Still follow this file's own deploy recipes (clean `origin/main` worktree, etc.) and log every such action per the Session logging convention below. Exception: `claude/native-applications-638ap0` (native Android) stays unmerged regardless of CI until a real local build succeeds locally at least once — see CHANGELOG.md's Android entries.
- **Clean up your own branch after every merge**: once a PR is squash-merged, delete the local branch (`git branch -D`) and remove any scratch `git worktree` used for the deploy. Remote branch deletion is automatic (repo Settings → General → "Automatically delete head branches" is enabled) — no agent-side remote-delete step is needed or possible (this sandbox's proxy 403s on `git push origin --delete`, and the GitHub MCP server has no delete-branch tool). There's a pre-existing backlog of ~50+ stale branches from before this setting existed — a one-time manual GitHub-UI sweep, not something to attempt from here.
- **Two agents work this repo, triggered manually by the account owner one at a time (never concurrently)**: Codex (branches `<hash>-codex/conduct-code-and-design-review-for-app`, opens its own PRs) and Claude Code sessions (branches `claude/<slug>`). Division of labor: **Codex writes feature/review PRs; a Claude session reviews them, fixes CI failures and real review findings, merges, deploys, and logs in CHANGELOG.md.** Push CI/review fixes directly to the Codex PR's own branch, don't open a competing PR. Because triggers are manual/one-at-a-time there's no real race condition, but each session inherits whatever `main` looked like when it was last invoked — **always re-fetch and check `git log origin/main` fresh at the start of any multi-agent-adjacent task.** **Rebase onto current `origin/main` before every new commit** (`git rebase origin/main`), not just when GitHub flags a conflict — a branch built on a commit since squash-merged shows a false "dirty" `mergeable_state`. `git reset --soft origin/main` is for a different, narrower case: only once a session's own branch has *already* been squash-merged and the session wants to start a fresh follow-up commit from a clean base — using it on a branch with real unmerged commits discards them instead of replaying them.
- **`claude-deploy@maxtr-c238f.iam.gserviceaccount.com` holds `roles/resourcemanager.projectIamAdmin`** — it (and therefore any agent session deploying under it) can grant itself any IAM role on this project, up to and including Owner. Use narrowly and deliberately: only to grant the specific missing role a deploy actually 403'd on (verify via a live `getIamPolicy` call first), never to pre-provision broader access "just in case." Log every self-granted role in this file.
  - **Self-granted, still held**: `roles/recaptchaenterprise.admin` and `roles/firebaseappcheck.admin` (kept permanently for future App Check management — see the App Check section below).

## Session logging convention

**Every session that takes a real action — a deploy, an IAM/config change, a merged PR, a fix — must record it in `CHANGELOG.md` (or a more specific doc like `AUDIT.md`) before ending the turn.** Follow the style already used throughout CHANGELOG.md: what was done, why, the exact commit/PR reference, and any gotcha hit — terse enough to skim, complete enough that a future session doesn't have to re-derive it from git log. Do this even for routine actions (a redeploy with "no changes detected", a docs-only PR). **CHANGELOG.md is a working log, not curated documentation** — it's intentionally separate from this file so CLAUDE.md itself stays a lean, current reference. If a fact from the log becomes permanent/architectural (a new convention, a new module, a recurring gotcha), promote it into the relevant section of this file — don't leave it buried in the log.

## Project overview

**Rytm** (formerly "Xamss", then "Zminka" — renamed to Rytm 2026-07-16) is a static PWA: a personal tracker for work shifts, multi-currency finances, and debt/settlement schedules. It's Ukrainian-first (`uk`/`en` toggle), backed by Firebase (Auth + Firestore + Cloud Messaging + one scheduled Cloud Function), and deployed via Firebase Hosting. `index.html` holds the markup/CSS and loads four small classic (non-module) scripts plus the module graph; almost all app logic lives under `js/*.js`, loaded via `<script type="module" src="./js/app.js">` (see "`js/` module layout" below).

## Commands

There is now a root `package.json` and a Vite build step (see "Build tooling" under Architecture) producing a `dist/` artifact that Firebase Hosting serves — but `js/*.js`/`index.html` are still hand-authored source, no framework. Playwright isn't a project dependency (installed globally in CI). Automated checks live under `tests/*.mjs`, all run in CI via `.github/workflows/smoke-test.yml`; anything beyond their coverage is verified manually with the recipe below.
- **Build**: `npm install && npm run build` — `vite build` (bundles `js/app.js`'s module graph into `dist/js/`) then `scripts/build-site.mjs` (copies the rest of the static site into `dist/`, patches `dist/index.html`'s script tag and `dist/sw.js`'s precache list). GitHub Pages does **not** use this — it serves the source tree directly, unbundled.
- **Type-check**: `npm run typecheck` (`tsc --noEmit`, `tsconfig.json`) — gradual, per-file opt-in via `// @ts-check` (project-wide `checkJs` is `false`). See "TypeScript adoption" under Architecture.
- **Lint**: `npm run lint` (`eslint .`, flat config at `eslint.config.js`) — scoped to `js/**/*.js`, excludes `js/vendor/**`. Correctness/hygiene net only (unused vars, undeclared globals, duplicate object keys, unreachable code) — deliberately **not** a style/formatter (no Prettier; this repo's existing hand-authored formatting is left alone). Every real global the app's own code relies on (bare `tr`/`Icon`/etc. from `js/classic-globals.js`, browser/DOM APIs) is declared explicitly in the config's `globals` block — if a genuinely new global is ever needed and `no-undef` flags it, add it there rather than disabling the rule. Runs in CI right after `typecheck`, same job. A first full-repo pass (2026-07-22) found exactly one real issue — a duplicate `common_cancel` key in `js/classic-globals.js`'s I18N object (harmless, both copies held the same string, but genuinely dead — fixed) — plus one intentionally-unused function (`canAddMore`, see "Premium / free-tier limits" below) suppressed with an inline `eslint-disable-next-line` and a comment explaining why, not silently ignored.
- **Run locally**: serve the repo root with any static file server (e.g. `python3 -m http.server`) and open `index.html`. Firebase Auth/Firestore calls hit the real `maxtr-c238f` project unless you intercept the `firebasejs` module imports.
- **Unit tests**: `node tests/unit.mjs` — plain-node tests for `functions/lib/pure.js` (currency conversion, timezone-aware date/hour resolution), split out of `functions/index.js` so they're importable without firebase-admin credentials.
- **Transaction validation tests**: `node tests/tx-validation.mjs` — plain-node tests for `js/tx-validation.js`'s `validateTransactionDraft()`; zero imports, no `AppState`/DOM, so importable in plain Node.
- **Smoke test**: `node tests/smoke.mjs` — serves `index.html`, stubs the Firebase SDK, signs in, dismisses onboarding, checks all 5 tabs render with no console/page errors. Not full coverage.
- **E2E CRUD test**: `node tests/e2e-crud.mjs` — drives the Finance tab's transaction form end to end: FAB modal → create → edit (via `#ui-dialog`/`#ui-dlg-ok` confirm) → delete → row gone. Covers the write path to the `transactions` Firestore subcollection.
- **CSV import/export round-trip test**: `node tests/csv-import-export.mjs` — export → delete → re-import, asserts the same amount/comment reappears; also checks an unknown-wallet row is reported via dialog, not silently dropped.
- **Shared profiles UI test**: `node tests/shared-profiles-ui.mjs` — client-wiring only (single stubbed uid): sharing shows a real invite code, redeeming your own/an unknown code surfaces a visible error toast. Real cross-account enforcement is covered separately by `tests/firestore-rules.mjs`'s shared-profiles section (see "Shared profiles" below for why split).
- **E2E modals test**: `node tests/e2e-modals.mjs` — covers the shared `.modal-overlay`/`closeManagers()` pattern generically.
- **Stress test**: `node tests/stress-tx-list.mjs` — seeds several hundred transaction docs, drives "view all", asserts every row renders and `content-visibility:auto` virtualization on `.tx-item` is live.
- **Targeted-update test**: `node tests/tx-list-targeted-update.mjs` — tags a `.tx-item`'s outer element, proves it survives (identity, not just equivalent content) an unrelated `renderFinance()` re-render, an add, and a delete.
- **Pull-to-refresh test**: `node tests/pull-to-refresh.mjs` — synthetic `TouchEvent`s: short pull snaps back, long pull past threshold triggers `fbLoadNow()` + toast, pulling while scrolled away from top does nothing.
- **Debt payoff-forecast chart test**: `node tests/debt-forecast-chart.mjs` — covers `js/debt.js`'s `renderDebtForecast()` (Preact-rendered): hidden state, correct values, and no stale `<strong>` elements left behind from a previous render.
- **CI**: `.github/workflows/smoke-test.yml` runs on every push/PR as **3** parallel jobs. `smoke`: `npm ci`, `npm run build`, `npm run typecheck`, `npm run lint`, installs Playwright globally, then runs the full `tests/*.mjs` list (currently: `unit`, `tx-validation`, `privacy-cache`, `functions-sweep`, `monobank-proxy`, `smoke`, `e2e-crud`, `e2e-modals`, `stress-tx-list`, `tx-list-targeted-update`, `pull-to-refresh`, `tx-swipe-delete`, `debt-swipe-delete`, `tx-list-sort-order`, `sheet-drag-dismiss`, `csp-handlers`, `pwa-launch-params`, `csv-import-export`, `ai-category-suggestion`, `shared-profiles-ui`, `shared-profile-permissions`, `reset-profile-data`, `monobank-connect`, `hide-amounts`, `debt-forecast-chart`) plus a `node --check` syntax pass on `functions/index.js`, against **source**. **This list drifts — verify against the actual workflow file, don't trust this sentence.** `dist-smoke` runs the same browser-driven tests a second time against the bundled `dist/` artifact. `firestore-rules` runs `tests/firestore-rules.mjs` against a real local Firestore emulator (JDK via `actions/setup-java`).
- **Firestore rules test**: `tests/firestore-rules.mjs` — asserts against a real emulator, not a read-through; has caught rules bugs invisible from the file itself (see CHANGELOG.md). For local iteration, run manually first with the emulator — see the script's header for the recipe.
- **Verify a change**: for anything the smoke test doesn't cover, drive `index.html` with Playwright against a **stubbed Firebase SDK** — intercept the `https://www.gstatic.com/firebasejs/.../firebase-{app,firestore,auth}.js` (and `firebase-messaging.js`/`firebase-app-check.js` if relevant) module imports via `page.route()` and fulfill with hand-written stubs, then exercise the UI with real clicks/fills. Always syntax-check the module script (extract with regex, `node --check` it) since `<script type="module">` bodies aren't validated by a naive whole-file check.
  - Operational notes: serve via `python3 -m http.server` (not `file://` — CORS). `playwright` is a global install, not a project dependency — import by absolute path (`/opt/node22/lib/node_modules/playwright/index.mjs`), not a bare specifier. Launch Chromium with `executablePath: '/opt/pw-browsers/chromium'`. Sign-in: make the stubbed `onAuthStateChanged(auth, cb)` fire `cb({uid, email})` itself; there is no `window.__authSignIn`. Dismiss onboarding (`window.finishOnboarding()` or click `#onboard-skip`) before interacting with tabs.
  - Stub auth needs every export `firebase-auth.js` uses, even ones not exercised by the test at hand — a missing export blanks the whole page.
  - `updateRatesOnline()` auto-fires on load and fails silently without network — the resulting console error is expected sandbox noise when asserting "no console errors."
- **Cloud Function deploy**: `cd functions && npm install && firebase deploy --only functions,firestore:rules` (Blaze plan required; see `SETUP.md` for one-time Console setup). **Cloud Functions run whatever was last deployed, not current git HEAD** — before writing "no functions changes this session," actually diff `functions/` against what's currently live (`git log <last-known-functions-deploy>..HEAD -- functions/`), don't reason from a commit message alone (this shipped a stale push-notification brand name for two days once — see CHANGELOG.md).
- **Hosting deploy**: `firebase deploy --only hosting --project maxtr-c238f` from a git worktree checked out at `origin/main`. `firebase.json`'s `hosting.predeploy` (`["npm install", "npm run build"]`) regenerates `dist/` automatically. Needs `roles/firebasehosting.admin` (separate IAM permission from the Functions deploy SA — expect a first-time 403). **GitHub Pages is also enabled** (Settings → Pages, `main`/root) and auto-deploys on push with no agent action — but the two mirrors diverge since the Vite adoption: `maxtr-c238f.web.app` serves the bundled `dist/`, `sudak666.github.io/Maxtr/` serves unbundled source. No tool available to an agent can flip Pages onto an Actions-based build — standing, known divergence.

## Architecture

### Layout
- `index.html` — markup, CSS, four classic `<script src>` tags plus `js/app.js` as the module-graph entry point (see "`index.html` script structure" below).
- `js/` — app logic, 16 native ES modules (no bundler needed for the source tree — browser resolves relative `import`s directly). See "`js/` module layout" below.
- `functions/` — separate Node 20 npm project (Cloud Functions v2, `firebase-admin`+`firebase-functions`), deployed independently.
- `firebase.json` / `firestore.rules` / `manifest.json` — Hosting, Firestore rules, PWA manifest.
- `sw.js` — the single service worker: PWA asset caching (network-first HTML, cache-first everything else) **and** FCM background push display, merged into one file because a scope can only have one active SW (there is no separate `firebase-messaging-sw.js` — don't recreate one). `FIREBASE_SDK_ASSETS` precaches the exact gstatic SDK URLs `index.html` statically imports (a static ES module import is all-or-nothing) — if the pinned SDK version in `index.html` changes, update this list and bump `CACHE_NAME`. `STATIC_ASSETS` lists every same-origin file needed for a cold offline start — add any new `js/*.js` file here too.
  **Review trap:** any PR that changes a file listed in `STATIC_ASSETS` or `FIREBASE_SDK_ASSETS` must include a root `sw.js` `CACHE_NAME` bump in the same commit/PR. Do not rely only on the `dist/` hash; already-installed PWA/TWA clients and the GitHub Pages mirror need the checked-in service worker byte to change.
- `privacy.html` / `terms.html` — standalone static pages, not part of the SPA.
- `SETUP.md` — manual, non-code Firebase Console setup (sign-in providers, authorized domains, VAPID key, GCP APIs). Keep in sync with new external dependencies.

### `index.html` script structure
Five `<script>` tags, in load order:
1. `js/theme-preinit.js` — applies saved theme before first paint (must stay a blocking classic script, first).
2. `js/touch-active-fix.js` — no-op `touchstart` listener; iOS Safari only applies `:active` CSS when at least one touch listener exists anywhere.
3. `js/sw-register.js` — registers `sw.js` on load, and drives auto-update: `controllerchange` reloads the page once when a new worker takes control, plus `reg.update()` on load/tab-refocus. Needed because the app is a long-lived PWA/TWA — without this, an already-open tab never picks up a deploy's new JS. Paired with `firebase.json` serving `sw.js`/`/js/**` as `Cache-Control: no-cache`. **When debugging "my fix isn't working on the phone," suspect stale JS delivery first** — the local test suite serves fresh files every run and is blind to this.
4. `js/classic-globals.js` — the monochrome SVG icon library (`ICON_PATHS`, `window.Icon(name)`, `setIcon`) plus I18N/`window.tr()`/`translateStaticDOM()`/`setLang()`/`setTheme()`, and a delegated-click listener for `.theme-opt`/`.lang-opt`/`.modal-card`/`.dlg-card`/`#profile-avatar-preview`. Runs before the module script and is genuinely outside the `js/` module graph (no `import`/`export`, no `__init_X__()`). `window.__applyLangDynamic` is the one hook back from `js/core.js` so `setLang()` can re-render dynamic content.
5. `<script type="module" src="./js/app.js">` — the whole `js/` module graph. Functions called from inline `onclick`/`onchange` attributes must be attached to `window` explicitly, since module-scope declarations aren't global — give it a real top-level declaration (`function foo(){}`) plus `window.foo = foo;`, not `window.foo = function(){...}` as the only definition (any other file that later needs to `import` it by name won't have one to import). **As of the CSP-hardening pass, inline `onclick=""` is the exception, not the rule** — see the CSP paragraph below; the ~10 deliberately-kept cross-file `window.*` exports are documented in CHANGELOG.md's onclick-removal-audit entries.

**A strict Content-Security-Policy IS deployed** (`firebase.json` hosting header): `script-src 'self' https://www.gstatic.com https://apis.google.com` — no `'unsafe-inline'`, no `'unsafe-eval'` (plus `https://www.google.com` for App Check's reCAPTCHA script and `'wasm-unsafe-eval'` for Tesseract's WASM — see those sections below). **Inline event-handler attributes silently do not execute on the live site**, and `eval`/`new Function` are blocked. The sandbox's `python3 -m http.server` sends no CSP, so inline handlers work in every local test and only no-op in production — this shipped real bugs before the onclick-removal audit (see CHANGELOG.md). **NEVER add an inline `onclick=""`/`onchange=""`** to `index.html` or a `js/*.js` template string — wire it as a `data-action` dispatched by the file's delegated capture-phase listener, or `el.onclick = fn` set in JS (a DOM-property assignment, not an inline attribute — CSP allows this). `tests/csp-handlers.mjs` (CI) loads the page under the real CSP and exercises these flows so this can't regress silently.

### `js/` module layout
`index.html` used to be one 7000-line inline `<script type="module">`; it's now 16 files under `js/` (`state.js`/`core.js`/`firebase-sync.js`/`color-picker.js`/`auth.js`/`app-init.js`/`ui-widgets.js`/`calendar.js`/`settings-managers.js`/`goals-profile.js`/`notifications.js`/`finance.js`/`analytics-csv.js`/`debt.js`/`shopping.js`, plus `app.js` as entry point) — a mechanical AST-verified split, not a rewrite. Three things matter for anyone splitting further or adding a cross-file call:

1. **All mutable app state lives in `AppState` (`js/state.js`)** — `transactions`, `wallets`, `shifts`, `currentUser`, `activeProfileId`, etc. (`AppState.x`, not per-field `export let x`), because many fields are reassigned from several different files and ES modules only let the declaring module reassign a plain binding. **`js/state.js` has zero imports of its own, deliberately** — a circular dependency back into it risks a temporal-dead-zone `ReferenceError` on its own top-level `const AppState = {...}`. **If you add a new module-level mutable field, put it in `AppState`, not a fresh top-level `let`** — a bare `let` can't be reassigned from another file.
2. **Every chunk with top-level side-effecting code is wrapped in an exported `__init_<chunk>__()`, called from `app.js` after all 16 files finish importing** — module evaluation order across a real (partly circular) import graph isn't guaranteed to match a single-file's original top-to-bottom order. **If you add new top-level side-effecting code** (`document.addEventListener`, `setInterval`, anything that isn't a `function`/`class`/`const`/`let` declaration), put it inside that file's `__init_X__()`, not bare at the top level.
3. **A name only ever exposed as `window.foo = function(){...}` still needs a real export/import if another file references the bare identifier `foo`** — the `window.foo = foo;` assignment line itself needs `foo` to already be a real lexical binding. **When adding a function only meant for inline HTML**, still give it a real top-level declaration plus a separate `window.foo = foo;` line (except `closeManagers`, which has a separate same-named bare wrapper with different code).

Verification for this split: `tests/smoke.mjs` + a manual Playwright pass (transaction add, settings modal, language switch, shift-detail modal) with zero console/page errors. Treat `js/*.js` as hand-edited source from here on, same as `index.html` always was — the original AST-analysis/codegen scripts weren't committed.

### Build tooling (Vite bundler)
Landed across several independently-verified, independently-deployed phases (full history in CHANGELOG.md); this section is the durable summary.
- **`vite.config.js` bundles only the ES module graph rooted at `js/app.js`** (`build.rollupOptions.input`, not `index.html`) — feeding Vite the whole `index.html` was tried first and found to fingerprint/rewrite `<link rel="manifest">`/`apple-touch-icon` paths, breaking GitHub Pages. The 4 classic scripts and `js/vendor/tesseract/**` are untouched by Vite.
- **`js/receipt-ocr.js`'s dynamic `import()` of vendored Tesseract needed `/* @vite-ignore */`** (so Vite never bundles the vendor library) and `entryFileNames`/`chunkFileNames` config forcing the bundled chunk under `dist/js/` (matching the source depth the relative specifier depends on).
- **`scripts/build-site.mjs`** (run via `npm run build`, after `vite build`) copies every static file Hosting needs into `dist/`, patches `dist/index.html`'s `<script type="module">` tag to the real bundle filename, and regenerates `dist/sw.js`'s `STATIC_ASSETS` from the actual build output — the "forgot to add a new file" `sw.js` bug class is now structurally impossible for `dist/`. `sw.js`'s `CACHE_NAME` for `dist/` hashes **every** precached file's bytes (not just the JS bundle's hashed filename), since non-hashed copied assets (manifest, icons) would otherwise leave the SW text byte-identical across deploys and never trigger an update.
- **`firebase.json`'s `hosting.public` is `dist`** (was `.`), with `hosting.predeploy: ["npm install", "npm run build"]` so a deploy always regenerates `dist/`.
- **GitHub Pages still serves the unbundled source tree** — standing, intentional divergence (`maxtr-c238f.web.app` = bundled `dist/`, `sudak666.github.io/Maxtr/` = raw source); no tool available to an agent can flip Pages' build-source toggle.
- **`window.__RYTM_TEST_HOOKS__`** (`js/app.js`, attached after every `__init_X__()`): `{ AppState, addTransaction, scanReceiptImage, setMonobankSyncGapMsForTesting }` — replaces the old `page.evaluate(() => import('./js/state.js'))` pattern, which only worked against unbundled source. **If a test needs one more module-scoped internal, add it to this hook**, don't reach for a dynamic `import('./js/...')` — it breaks against `dist-smoke`.
- **CI has 3 jobs**: `smoke` (source), `dist-smoke` (bundled `dist/`, full parity via the test-hooks fix above), `firestore-rules` (emulator, unrelated).

### TypeScript adoption (gradual, JSDoc-based)
Type-checking plain `.js` files via inferred types + JSDoc, zero runtime change, zero file renames — the natural low-risk followup to the Vite work.
- **`tsconfig.json`**: `allowJs: true`, **`checkJs: false` project-wide** (`include: ["js/**/*.js"]`), `noEmit: true` (this never emits — Vite's esbuild still does the real build). Deliberate, not a placeholder — a global `checkJs: true` sweep against zero prior annotations would flood with errors.
- **Per-file opt-in via `// @ts-check` at the top of the file** — a standard TypeScript feature. **The entire `js/*.js` module graph now opts in** (as of 2026-07-22 — `js/app.js`/`js/state.js`/`js/monobank.js` were the last 3, see CHANGELOG.md for the full per-file history of casts/ambient-type gotchas hit along the way). Deliberately still uncovered: the 4 classic non-module scripts (`theme-preinit.js`/`touch-active-fix.js`/`sw-register.js`/`classic-globals.js` — optional, lower-priority, not part of the module graph) and `js/vendor/**` (Preact/Tesseract, never meant to be typed). On the `functions/` side: `functions/lib/pure.js` and `functions/lib/monobank.js` (needed their own `functions/tsconfig.json` — CommonJS, `module:"CommonJS"`, scoped to `include:["lib/**/*.js"]`, omit `moduleResolution` — TS7 removed `Node10`).
- **Correction**: `functions/lib/sweep.js` already carries `// @ts-check` too (this section previously called it out as the still-unopted "next candidate," which was stale — it passes the loose `strict:false` config cleanly, same as every other opted-in file; what actually remains for it is the strict-mode pass below, and it's a real one — coupled to firebase-admin's Firestore snapshot types and the finance doc's complex shape, several genuine `possibly undefined`/implicit-`any` gaps once checked under `strict:true`).
- **`npm run typecheck`** runs all 4 configs (`tsc --noEmit && tsc --noEmit -p functions/tsconfig.json && tsc --noEmit -p tsconfig.strict.json && tsc --noEmit -p functions/tsconfig.strict.json`), in CI right after the build step.
- **Strict-mode pass, started 2026-07-22**: opting into `@ts-check`/loose `checkJs` catches only gross errors (wrong property names, obvious type mismatches) — a real strict audit (`strict:true`: `noImplicitAny`, `strictNullChecks`, etc.) against the full already-opted-in `js/*.js` graph turned up **1211 errors** in one pass (dominated by implicit-`any` parameters and unchecked-`null`/`undefined` access — see CHANGELOG.md for the full breakdown). Since `strict` is a project-wide `tsconfig.json` flag, not a per-file pragma like `@ts-check`, it can't be adopted file-by-file inside the *existing* config the way `@ts-check` was — instead, **`tsconfig.strict.json`** (root) and **`functions/tsconfig.strict.json`** are separate configs, each `strict:true`, each with a narrow `include` naming only the specific files already verified clean under strict mode. Growing that `include` list file-by-file, each addition verified for real (not `as any`-cast into silence), is the same incremental discipline the `@ts-check` rollout itself used — this is **not** a "flip strict:true globally and grind through 1211 errors" project; rushing that on a live financial app is exactly the outcome to avoid.
  - **Currently in the strict set**: root — `js/tx-validation.js`, `js/privacy-cache.js`, `js/state.js`, `js/receipt-ocr.js`, `js/dashboard-widgets.js`, `js/ui-widgets.js`, `types/globals.d.ts`; `functions/` — `lib/pure.js` (one real fix: `SEED_RATES` needed an explicit `RatesMap` JSDoc type annotation so indexing it by a dynamic currency-code string type-checks honestly, instead of TS inferring a closed object type), `lib/monobank.js` (passed with no changes needed).
  - **Critical scoping rule, learned the hard way**: `tsconfig.strict.json`'s `include` list does *not* bound what gets checked — `checkJs:true` recursively checks every file reachable via a real ES `import`, not just the files named in `include`. A file can only safely join the strict set once its **entire transitive import closure** is already strict-clean (or excluded). This means the right ordering metric is "how many non-strict files does this pull in," not "how many errors does this file have on its own" — `js/shopping.js` looked like a great next candidate (~20 own errors) but actually imports `color-picker.js`/`settings-managers.js`/`ui-widgets.js` (100+290+68 errors respectively), so adding it balloons back to 1000+ errors instantly; reverted. `js/app-init.js` is even worse — it's the app's own init/orchestration file and imports nearly every other module, disqualifying it entirely until most of the graph is already strict. **Only files whose closure is state.js (zero imports) plus already-strict-clean files, or vendor code, currently qualify.**
  - **Two related gotchas fixed while getting there**: (1) `types/globals.d.ts`'s `window.__RYTM_TEST_HOOKS__` declaration used `typeof import('../js/state.js').X`-style type references, which — same rule as above — transitively pulled in the whole graph the moment `types/**/*.d.ts` was named in `include`. Fixed by splitting it into a separate `types/test-hooks.d.ts` (TypeScript merges `declare interface Window {...}` across files automatically, so this needed no other change) — `tsconfig.strict.json` includes only `types/globals.d.ts` now. (2) `js/dashboard-widgets.js` imports the vendored `js/vendor/preact/preact.module.js` directly; `exclude: ["js/vendor/**"]` does **not** stop a file reached via a real `import` from being checked under `checkJs:true` (exclude only filters glob-discovered root files, not transitively-resolved ones) — fixed with a literal `// @ts-nocheck` pragma prepended to that one vendored file, the correct sanctioned way to force-skip a specific file regardless of the project's `checkJs` setting.
  - **Real fixes needed along the way** (beyond the two gotchas above): `js/receipt-ocr.js`'s `canvas.getContext('2d')` can genuinely return `null` — added a graceful fallback (skip downscaling, pass the original file through) matching the function's existing degrade-gracefully convention, rather than asserting non-null. `js/state.js`'s `window.currentLang` was typed as a bare `string`; narrowed to the real `'uk'|'en'` union (matches what `js/classic-globals.js` actually ever assigns) so `LANG_CALENDAR[window.currentLang]` indexes honestly — this in turn revealed a genuine (if harmless, JS coerces it) pre-existing sloppiness in `js/app-init.js`: `option.value = i` assigned a `number` where DOM expects `string`, previously masked because the indexing bug made `AppState.MONTHS` infer as `any`. Fixed with `String(i)`.
  - **`js/ui-widgets.js` added (67 own errors, real fixes throughout)**: only imports `state.js` (already strict), confirmed by grepping every file's own `import` list before attempting it — the corrected version of the "which file next" question the `js/core.js` guess below got wrong. Needed the full range of this session's established patterns (DOM-element casts, a `Record<string, (ds: DOMStringMap) => void>` type for every `CLICK_ACTIONS`/`FIELD_ACTIONS` dispatch map, `AppState.__dlgResolve` given a real function-or-null type in `js/state.js`) plus one new one: a parameter narrowed non-null by an early-return guard (`if(!sel || ...) return;`) re-widens back to nullable inside a *nested function* defined later in the same outer function — fixed by capturing the guard's result into a fresh `const el = sel;` right after the guard and using `el` (not the original parameter) everywhere inside the closures, in `enhanceSelect`/`enhanceDateInput`/`initSegThumb`. Also had a JSDoc-authoring mistake caught by the type-check itself, not by inspection: two separate `/** ... */` comment blocks stacked back-to-back on `uiDialog` (one `@param`, one `@returns`) only associates the *second* one — TypeScript silently drops the first, re-widening every destructured param back to implicit `any`. Fixed by merging into one block; this is worth remembering as a category of mistake since it produces no syntax error, just quietly wrong types. **Real cross-file ripple**: giving `uiDialog`'s return type real precision surfaced actual `.trim()`-on-a-possibly-boolean bugs in `js/finance.js`/`js/settings-managers.js` wherever they called `uiPrompt()`/`uiAlert()` and assumed a bare string back — fixed by giving `uiAlert`/`uiPrompt` their own precise return types (`Promise<boolean|null>`/`Promise<string|null>`) reflecting what each dialog variant actually resolves to, rather than inheriting `uiDialog`'s wider three-way union. Also fixed 3 unrelated call sites in `js/goals-profile.js`/`js/calendar.js`/`js/settings-managers.js` that passed a loosely-typed `Element`/`HTMLElement` into `enhanceSelect()`/`initSheetDrag()`'s now-precise parameter types.
  - **`js/core.js` looked like the obvious next candidate (~50-54 own errors) but is actually disqualified**: it imports `analytics-csv.js`/`calendar.js`/`debt.js`/`finance.js`/`goals-profile.js`/`notifications.js` — all large, non-strict hub files — so adding it reinflates the check to 1000+ errors exactly like `shopping.js`/`app-init.js` did. **Before proposing any file as "next," grep its own `import` list first** and confirm every one of those files is either already in the strict set or has zero imports of its own — own-error-count alone is not a reliable signal, core.js proves that as much as shopping.js did.
  - **`js/core.js`'s own 54 errors fixed for real, but the file still cannot join `tsconfig.strict.json`'s `include`** — real fixes: `AppState.currentUser` typed `import('firebase/auth').User | null` (was inferring bare `null`, so every `.uid` read after a truthy check narrowed to `never`), `AppState.wallets`/`shiftTypes` given real `Wallet`/`ShiftType` typedefs (were inferring `never[]` from the empty-array literal — same class of gap `ShoppingItem` had already solved for `shoppingList`), `AppState.categoryIcons`/`currencyRates` given `Record<string,string>`/`Record<string,number>` types (were closed `{}` types rejecting any dynamic-string index), `AppState.transactions`/`recurring` widened `never[]`→`any[]` (a real typedef wasn't worth the risk yet, see below). Every implicit-`any` parameter across `logAppError`/`sanitizeWidgetOrder`/`canAddMore`/`showPremiumUpsell`/`categoryColor`/`__applyLangDynamic`/`categoryIcon`/`currencySymbol`/`convertCurrency`/`toBase`/`walletCurrency`/`txCreatedAt`/`compareTransactionsNewest`/`shiftType`/`walletById`/`subKey` given real JSDoc types; several object-literal lookup tables (`WIDGET_SECTION_IDS`/`FREE_LIMITS`/`PREMIUM_LIMIT_MSG_KEYS`/`CAT_ICON`/`CURRENCY_META`/`SEED_RATES`) given `Record<string,...>` annotations instead of inferring closed shapes; `applyWidgetOrder()`'s `anchor.parentNode` re-widened-to-nullable-inside-a-closure bug fixed with the same capture-into-a-fresh-`const` pattern `ui-widgets.js` established; `walletCurrency()` rewritten from `(walletById(id)||{}).currency||'UAH'` to `walletById(id)?.currency || 'UAH'` (a real correctness/clarity improvement, not just a type fix); `types/globals.d.ts`'s `__applyLangDynamic` signature narrowed `(lang:string)=>void`→`(lang:'uk'|'en')=>void`, matching the same real-world-values reasoning already applied to `window.currentLang`.
  - **Even with `core.js` itself fully clean, it still can't be added to `include`**: it imports `analytics-csv.js`/`calendar.js`/`debt.js`/`finance.js`/`goals-profile.js`/`notifications.js` — 6 more non-strict hub files, transitively pulling in `settings-managers.js`/`color-picker.js`/`auth.js`/`firebase-sync.js`/`app-init.js` too (~750+ errors total across that cluster, confirmed by temporarily adding `core.js` to a scratch copy of `tsconfig.strict.json` and inspecting the real error count — reverted before committing). At this point essentially the *entire* remaining non-strict graph is one interconnected cluster (`core.js`↔`color-picker.js`↔`firebase-sync.js`↔`notifications.js`↔the rest) — there is no remaining leaf file left to pick off one at a time; real further progress means taking on a large multi-file batch, which is deliberately **not** attempted in one sitting (see the account owner's explicit no-mass-casting stance above). `core.js`'s own fixes were kept anyway under the existing loose `@ts-check` config, same precedent as `shopping.js`'s partial fixes in batch 2.
  - **A new, real config-completeness gap found along the way**: `types/firebase-vendor.d.ts` (the ambient module mapping for the pinned `gstatic` Firebase CDN URLs) is not in `tsconfig.strict.json`'s `include`, so any file importing those URLs (starting with `core.js` itself) shows 4 spurious `Cannot find module` errors under the strict config specifically — the loose root `tsconfig.json` doesn't hit this since its `include` already covers `types/**/*.d.ts` wholesale. Confirmed adding `types/firebase-vendor.d.ts` to a scratch strict config resolves exactly those 4 errors with zero other fallout (it only re-exports the `firebase` npm package's own types, no transitive graph risk) — not committed yet since nothing in the real strict set needs it until `core.js` (or another gstatic-importing file) actually joins, but the next session adding `core.js` for real needs this in the same PR.
  - **`js/color-picker.js`'s own 74 errors also fixed for real, same "fix but don't add to `include`" treatment** — it imports `app-init.js`/`calendar.js`/`core.js`/`debt.js`/`finance.js`/`firebase-sync.js`/`goals-profile.js`/`notifications.js`/`settings-managers.js`/`ui-widgets.js`, i.e. it's *the* hub at the center of the remaining cluster, not a step toward escaping it. Real fixes: `js/state.js` gained a `ProfileMeta` typedef (`id`, `name`, plus the optional `avatar`/`createdAt`/`kind`/`ownerUid` fields real profile rows carry — `profilesMeta.list`'s literal-seed inference had closed the shape to just `{id,name}`) and a `Tag` typedef, plus real types for `categories`/`budgets`/`subcategories` (`Record<string,...>`, were closed `{income:[],expense:[]}`/`{}` shapes rejecting a dynamic-string index), `tags`/`selectedTagIds`, `colorPickTarget`/`avatarPickTargetProfileId`, `lastKnownUpdatedAt`, `activeProfileOwnerUid` (`string|null`, was inferring bare `null` the same way `currentUser` had), `debts` (widened `never[]`→`any[]`, same treatment as `transactions`/`recurring`), and `fbTimer` (`ReturnType<typeof setTimeout> | null` — was bare `null`, so every `clearTimeout(AppState.fbTimer)` call site errored; fixed at the 2 call sites in this file with `?? undefined`, since `clearTimeout` accepts `number|undefined`, not `null`, even though `null` is a harmless no-op at runtime). All implicit-`any` dispatch-map parameters (`fbSetStatus`, `computeNextDate`, the `add()` closure in `backfillCategories`, `renderSharedMembersList`/`toggleMemberRoleUI`/`switchProfile`/etc.) given real JSDoc types; a `let currentManagedMembersProfileId=null` module-level variable typed explicitly (was inferring differently at different reassignment sites, TS7034/TS7005); the file's `CLICK_ACTIONS` dispatch map typed `Record<string, (ds: DOMStringMap) => void>` matching `ui-widgets.js`'s established precedent, with each handler's DOM-sourced string guarded (`ds.id||''`) at the call site rather than making every downstream function's parameter optional — kept each function's own signature honestly "always needs a real id to do anything," matching this file's existing `||''`-guard convention elsewhere, rather than the `ui-widgets.js` precedent of loosening the callee's parameter (that precedent fit its one single already-fallback-tolerant case; blanket-applying it here across 8 different functions would have been the wrong generalization). Two real `document.getElementById(...).style.display=` null-safety bugs fixed the same way `core.js`'s batch fixed its own.
  - `sw.js` `CACHE_NAME` bumped `v103`→`v104` (real logic, bundle hash moved).
  - No leaf file remains in the strict-set's closure requirements as of this batch either — `color-picker.js` and `core.js` are now both individually clean but both still blocked by the same shared cluster of 10 hub files (`analytics-csv.js`/`app-init.js`/`auth.js`/`calendar.js`/`debt.js`/`finance.js`/`firebase-sync.js`/`goals-profile.js`/`notifications.js`/`settings-managers.js`, ~500+ errors combined). The next contained step is picking off one more of those hub files' own errors the same way (`settings-managers.js` is the largest at ~170, `finance.js` next at ~105) — or, once enough of the cluster's individual files are clean this way, attempting the real multi-file `include` batch in one sitting.
  - **`js/firebase-sync.js` picked next specifically because re-checking every remaining file's real error count after batches 4-5's `state.js` typing fixes found it had dropped to just 8 own errors** (was previously estimated ~19-34 in earlier passes, before `ProfileMeta`/`activeProfileOwnerUid`/`currentUser` were properly typed) — confirmed by re-running the same scratch-config technique across all 11 remaining files at once rather than trusting stale per-file estimates from prior sessions. Real fixes: `AppState.activeProfileOwnerUid=first.kind==='shared'?first.ownerUid:null` needed `first.ownerUid??null` (`ProfileMeta.ownerUid` is `string|undefined`, the target field is `string|null` — a real, if narrow, type mismatch, not a false positive); `loadActiveProfileRole()`'s `roles[AppState.currentUser.uid]` given a real null-safe `AppState.currentUser && AppState.currentUser.uid` read (fails open to `'editor'` when signed out, matching this function's own already-documented fail-open philosophy); `loadTransactionsFromSubcollection()`/`deleteAllTransactionDocs()`'s `out`/`ids` arrays given explicit `any[]`/`string[]` types. **A real root-cause fix, not just call-site patches**: `userDoc(name)` had no `@returns` JSDoc annotation, so TypeScript's overload resolution for the `collection()`/`getDocs()` chain built on top of it silently degraded — its two return statements both return `doc(db,'users',...)` so the inferred return type should have been unambiguous, but without an explicit annotation the downstream `snap.forEach(d=>...)` calls still came back with `d: any` (implicit-any) rather than the real `QueryDocumentSnapshot` type; added `@returns {import('firebase/firestore').DocumentReference}` to `userDoc()` (a real, honest fix — the function's two return paths already both produce exactly that type) plus inline `(/** @type {...} */ d)` casts on the 2 remaining `.forEach()` callbacks Firestore's own overloaded types still couldn't resolve cleanly even with `userDoc()` now annotated.
  - `sw.js` `CACHE_NAME` bumped `v104`→`v105` (real logic, bundle hash moved: `app-iMwE-hV9.js`→`app-C8X9uhJ0.js`).
  - **`js/firebase-sync.js` is individually strict-clean but not added to `include`** — same treatment as `core.js`/`color-picker.js`: it imports `core.js`, which itself still transitively pulls in the remaining 9-file cluster. Confirmed via the same scratch-config check. **Re-running the full 11-file error count after this fix**: `settings-managers.js` (127), `finance.js` (68), `analytics-csv.js` (61), `goals-profile.js` (53), `auth.js` (51), `debt.js` (42), `calendar.js` (35), `app-init.js` (20), `notifications.js` (8) — `notifications.js` is now the next-smallest candidate (imports `state.js`/`color-picker.js`/`core.js`/`ui-widgets.js`, all either already-strict or already individually-clean-but-unlisted), a strong next pick by the same "smallest own-error-count among files whose imports are already resolved" heuristic that made `firebase-sync.js` this batch's pick.
  - **`js/notifications.js`'s own 8 errors fixed for real (batch 7)**: `showLocalNotification(title, body)` given real `@param {string}` types; the `show` closure's `reg` parameter typed `ServiceWorkerRegistration | null | undefined` (both — `null` for the explicit no-registration branch, `undefined` because `navigator.serviceWorker.getRegistration()`'s real Promise resolves `ServiceWorkerRegistration | undefined`, not `| null`, a genuine two-different-absent-value-types gotcha caught by the type-checker, not by inspection); `onMessage()`'s `payload` callback given an inline JSDoc-cast param type. `enablePushNotifications()`/`disablePushNotifications()` both gained a real `AppState.currentUser` null-guard before reading `.uid` for the `push_tokens` doc path (the push-enable flow already assumes signed-in, so this is a genuine no-op guard for an unreachable-in-practice case, not a behavior change). `FIELD_ACTIONS` typed `Record<string, (ds: DOMStringMap, el: HTMLElement) => void>` with the same `action=el&&el.dataset.action` capture-before-index pattern established for `CLICK_ACTIONS` maps elsewhere.
  - `sw.js` `CACHE_NAME` bumped `v105`→`v106` (real logic, bundle hash moved: `app-C8X9uhJ0.js`→`app-DCQJfqfE.js`).
  - **`js/notifications.js` is individually strict-clean but not added to `include`** — same cluster-membership reasoning as every file in this batch series (imports `color-picker.js`/`core.js`, both individually clean but still pulling the rest of the cluster). **Fresh full-cluster count after this fix**: `settings-managers.js` (127), `finance.js` (68), `analytics-csv.js` (61), `goals-profile.js` (53), `auth.js` (51), `debt.js` (42), `calendar.js` (35), `app-init.js` (20) — `calendar.js` and `debt.js` are now the two remaining leaf-like files (each imports only `state.js`/`color-picker.js`/`core.js`/`ui-widgets.js`, `debt.js` also `js/vendor/preact/preact.module.js` which is already `@ts-nocheck`'d), making `calendar.js` (35, smaller) the next candidate by the same heuristic.
  - **`js/calendar.js`'s own 35 errors fixed for real (batch 8)**: `AppState.shifts` given a real `Record<string, string[]>` type (was inferring bare `{}`, rejecting the dynamic date-string keys every calendar function indexes it by — the single fix that resolved roughly half this file's errors on its own) and `AppState.selectedDateKey` typed `string | null` (was bare `null`). A real correctness fix along the way: `saveModalSelection()`'s checkbox-collection loop used to `push()` `el.getAttribute('data-stid')` (`string | null`) straight into the shifts array with no null guard — harmless in practice since every rendered checkbox always carries the attribute, but a genuine latent gap now closed with an explicit `if(v) al.push(v)`. Every other implicit-`any` local (`renderShiftModalOptions`/`shiftCode`/`smoothPath`/`fmt`/the `S()` stats-DOM helper/`dk()`/`autoFillTypeForDate`/`toggleAutoFill`) given real JSDoc types; `SHIFT_PATTERN_CYCLES` typed `Record<string, number[]>`; `CLICK_ACTIONS`/`FIELD_ACTIONS` typed with the same `Record<string, (ds: DOMStringMap[, el]) => void>` + capture-before-index pattern used throughout this rollout.
  - **No dedicated test file covers the calendar/shift-add flow** (confirmed by grepping `tests/*.mjs` for `shift-modal`/`calendar-grid`/`autofill`/`day-cell` — zero matches) — since this batch touched real logic on that exact path (the `AppState.shifts` write), a throwaway Playwright script (not committed) drove the real flow: open a day cell → check a shift-type checkbox → save → assert a real `.shift-token` renders → uncheck → save → assert it's gone. All verified against real behavior, not just `npm run typecheck`.
  - `sw.js` `CACHE_NAME` bumped `v106`→`v107` (real logic, bundle hash moved: `app-DCQJfqfE.js`→`app-B-io7ojz.js`).
  - **`js/calendar.js` is individually strict-clean but not added to `include`** — same cluster reasoning as every file in this series (imports `color-picker.js`/`core.js`). **Fresh full-cluster count**: `settings-managers.js` (122, dropped from 127 — the `AppState.shifts` typing fix benefited it too), `finance.js` (68), `analytics-csv.js` (61), `goals-profile.js` (53), `auth.js` (51), `debt.js` (42), `app-init.js` (20) — `debt.js` is now the only remaining leaf-like file (imports only `state.js`/`color-picker.js`/`core.js`/`ui-widgets.js` plus the already-`@ts-nocheck`'d vendored Preact module), the clear next candidate.
  - **`js/debt.js`'s own 42 errors fixed for real (batch 9), and this closes out the "leaf files" phase of the rollout** — after this, no remaining non-strict file imports only already-strict-or-individually-clean files; `goals-profile.js`/`auth.js`/`app-init.js`/`analytics-csv.js`/`finance.js`/`settings-managers.js` all mutually import each other (confirmed by grepping every one of their own `import` lists in one pass, not assumed), forming one final 6-file cyclic cluster with no more one-at-a-time picks available.
  - **Real fixes**: a root-cause fix in the same style as `firebase-sync.js`'s `userDoc()` — `getCurrentDebt()` had no `@returns` annotation, so every caller's local `cd` variable came back untyped, cascading implicit-`any` through half the file; added local `Debt`/`DebtEntry` typedefs and `@returns {Debt | undefined}`, which alone resolved most of the downstream errors in every function that starts with `const cd=getCurrentDebt(); if(!cd) return;`. `AppState.currentDebtId` typed `any` (debt ids are numeric in practice but the field is read/written across several files) and `AppState.debtEntryEditId` typed `number | null` (both were bare `null`). Every remaining implicit-`any` local/parameter given real types (`switchDebt`, `debtCurrentBalance`, `renderDebtDueChip`, `renderDebtProgress`, `renderDebtForecast`, the `x`/`y` burndown-chart coordinate closures, `toggleDebtEntryEdit`, `closeAllDebtRowSwipes`, `setupDebtRowSwipe`, the 2 `S()` stats-DOM helpers, `dispatchFieldAction`); `DebtBurndownChart`'s Preact props destructure given a real inline typedef. `CLICK_ACTIONS`/`FIELD_ACTIONS` typed with the same established dispatch pattern. Two genuine `parseFloat(possibly-undefined-string)` gaps fixed with `||''` fallbacks (matching the existing convention elsewhere), and `updateDebtEntry()`'s dynamic-field write (`e[field]=...`, writing to `amount`/`balance`/`date` by a runtime string) kept as an explicit `/** @type {any} */` cast rather than force-fit into the closed `DebtEntry` shape — an honest cast for genuinely dynamic property access, not a type-error silencer.
  - `sw.js` `CACHE_NAME` bumped `v107`→`v108` (real logic, bundle hash moved: `app-B-io7ojz.js`→`app-BmRQ8Oq5.js`).
  - **`js/debt.js` is individually strict-clean but not added to `tsconfig.strict.json`'s `include`** — same cluster reasoning as every file in this series (imports `color-picker.js`/`core.js`). **Fresh full-cluster count**: `settings-managers.js` (122), `finance.js` (68), `analytics-csv.js` (61), `goals-profile.js` (53), `auth.js` (51), `app-init.js` (20) — with `debt.js` now resolved, this is the complete remaining set, and it's a genuine cycle (each of these 6 imports at least one of the others). No more "which leaf is smallest" question to answer; the next contained step is picking one hub's *own* errors the same "fix for real, don't add to `include` yet" way `core.js`/`color-picker.js`/`debt.js` already got (smallest by own-error-count: `app-init.js` at 20, though its role as the app's central orchestrator makes it a less natural target than a mid-size hub like `finance.js` or `analytics-csv.js`) — or, once several more of the 6 are individually clean this way, attempting the real multi-file `include` batch this file has flagged as the eventual endgame since batch 2.
  - Verified: `npm run build`/`npm run lint`/`npm run typecheck` all clean; `tests/unit.mjs`, `smoke.mjs`, `debt-forecast-chart.mjs`, `debt-swipe-delete.mjs` all green (the two dedicated Debt-tab tests both cover real logic this batch touched — the Preact burndown chart and the swipe-to-delete/reorder list — so this file had better direct test coverage than `calendar.js` did in the previous batch).
  - **`js/analytics-csv.js`'s own 61 errors fixed for real (batch 10)** — picked as the mid-size hub CLAUDE.md's own prior entry flagged as the more natural next target over `app-init.js` (the orchestrator). New local `Transaction` typedef (`id`/`type`/`date`/`amount`/`currency`/`category`/`subcategory`/`wallet`/`targetWallet`/`targetAmount`/`targetCurrency`/`comment`/`tags`/`createdAt`) used throughout, same "local typedef, don't touch `AppState.transactions`'s own `any[]`" treatment `debt.js`'s `Debt`/`DebtEntry` got. `analyticsPredicates()` given a real tuple `@returns` type (`[(t: Transaction) => boolean, ((t: Transaction) => boolean) | null]`) — the previously-untyped return let TS infer the first tuple element as possibly-null too (unioned across all `return` statements), which was the root cause of 2 `Cannot invoke an object which is possibly 'null'` errors at the call site, not a call-site problem at all.
  - **The same closure-narrowing gotcha `ui-widgets.js`/`debt.js` already established, in a new shape**: `renderFinance()`'s targeted-DOM-update loop declared `let item=existingById.get(idStr)` (typed `HTMLElement|undefined` once the `Map` got a real `Map<string,HTMLElement>` generic) then reassigned it inside an `if(isNew)` branch — the reassignment narrowed `item` for immediate statements in that block, but not inside the `click`/`keydown` listener closures registered in the same block, nor after the `if/else` merged back together (TS can't correlate the separately-named `isNew` boolean back to `item`'s own null-check). Fixed by building the new node into a fresh `const el` inside the branch (used by the closures) and assigning `item=existing`/`item=el` only once each branch is done — the same "capture into a fresh const, use it inside closures" pattern, just needed on both sides of an if/else this time instead of after a single guard.
  - **A real `TransactionDraft` type mismatch, not a false positive**: `buildTransactionsFromCSV()`'s row-parsing built `{wt:targetWalletId, cat:...||null, sub:...||null}`, but `tx-validation.js`'s `TransactionDraft` typedef declares `wt`/`cat`/`sub` as optional (`string` when present, simply *absent* when not) — `null` is a different, not-interchangeable "value" as far as that type goes. Fixed by converting to `undefined` (`||undefined` instead of `||null`) at the 3 sites building that object, matching what the shared typedef actually expects; `finance.js`'s own analogous draft-building code has the identical `null` pattern and will need the same fix whenever it's `finance.js`'s turn (flagged, not fixed here — out of scope for this file).
  - Every remaining implicit-`any` local/parameter given real types across the rest of the file (`setAnalyticsPeriod`, `mPrefix`, `byCatAmount`, `renderCatList`, `computeWalletBalances`'s `wb` map, `txItemInnerHtml`, `closeAllTxSwipes`, `setupTxSwipe`, the CSV `csvEscape`/`parseCSV`/`buildTransactionsFromCSV`/`handleImportCSVFile` pipeline, `walletName`); `TX_TYPE_LABEL`/`CSV_TX_TYPE_FROM_LABEL` lookup tables given `Record<string,string>` types; `CLICK_ACTIONS`/`FIELD_ACTIONS` typed with the established dispatch pattern (this file's variant passes the raw event through to a couple of handlers too, typed `(ds: DOMStringMap, e?: Event) => void` / `(ds, el, e?) => void` to match).
  - `sw.js` `CACHE_NAME` bumped `v108`→`v109` (real logic, bundle hash moved: `app-BmRQ8Oq5.js`→`app-DYQ1J3kB.js`).
  - **`js/analytics-csv.js` is individually strict-clean but not added to `tsconfig.strict.json`'s `include`** — same cluster reasoning as the whole series (imports `calendar.js`/`finance.js`/`firebase-sync.js`/`goals-profile.js`/`settings-managers.js`, several of which are themselves still non-strict). Remaining cluster: `settings-managers.js` (122), `finance.js` (68), `goals-profile.js` (53), `auth.js` (51), `app-init.js` (20).
  - **This is the widest-blast-radius file fixed so far in this rollout** (the transaction-list rendering/targeted-update engine, CSV import/export, and the analytics donut widget all live here), so verified unusually broadly: `npm run build`/`lint`/`typecheck` clean, plus `tests/unit.mjs`, `smoke.mjs`, `e2e-crud.mjs`, `csv-import-export.mjs`, `tx-swipe-delete.mjs`, `stress-tx-list.mjs`, `tx-list-targeted-update.mjs`, `tx-list-sort-order.mjs`, `analytics-donut-chart.mjs` — all green, including the specific targeted-DOM-update regression test for the exact `item`-narrowing rewrite above.
  - **`js/finance.js`'s own 68 errors fixed for real (batch 11)** — the transaction-add/edit form, tags manager, auto-rules manager, on-device AI category suggestion, and receipt-scan wiring. `js/state.js` gained a new `AutoRule` typedef (`id`/`type`/`keyword`/`category`) and `AppState.autoRules` was retyped from inferred `never[]`→`AutoRule[]`, resolving the single largest error cluster in this file on its own (every auto-rule-management function had been reading properties off `never`). `AppState.editingTxId` typed `string | number | null` (was bare `null`) and `AppState.expandedRuleId` typed `string | null` (was bare `null`), matching the same "empty/null literal infers too narrow" pattern this rollout keeps hitting.
  - **The same `TransactionDraft` null-vs-undefined gap flagged (but left unfixed) in batch 10's CSV-import code turned out to have a second, in-file occurrence**: `readTransactionForm()`'s `sub` field was built as `...?.value||null`, same mismatch against `tx-validation.js`'s `TransactionDraft` typedef (`sub` wants a real `string` or to be *absent*, not `null`) — fixed the same way, `||undefined`.
  - **Two dynamic-field writes on closed typedefs**, same treatment as `debt.js`'s `updateDebtEntry()` precedent: `updateTag()`'s `t[field]=value` and `updateAutoRule()`'s `r[field]=value` both cast to `/** @type {any} */ (t)[field]=value` — an honest cast for genuinely dynamic runtime property names, not a type-error silencer.
  - Module-level `let aiSessionPromise=null`/`let aiSuggestTimer=null` (the on-device AI category-suggestion debounce/session state) hit the same TS7034 "implicitly has type any in some locations" pattern as `color-picker.js`'s `currentManagedMembersProfileId` — fixed with explicit `Promise<any> | null` / `ReturnType<typeof setTimeout> | null` annotations; the timer's `clearTimeout()` call needed the same `?? undefined` fix `fbTimer` needed in batch 5 (`clearTimeout` wants `number|undefined`, not `null`).
  - The receipt-scan `catch(err){...}` block read `err.message` off a caught exception of unclear type — fixed with a real `err instanceof Error` narrowing guard rather than a cast, since the check is genuinely meaningful (a non-Error throw is possible from the OCR worker path).
  - Every remaining implicit-`any` local/parameter given real types (`toggleFinTag`, `sameTxId`, `editTransaction`, `setTxAmount`, `deleteTag`'s `affected` array and its filter callback, `toggleRuleEdit`, `updateAutoRule`, `deleteAutoRule`, `handleReceiptFile`); 2 `document.getElementById(...).style.display=` calls (`openTagsManager`/`openRulesManager`) null-guarded the same way `core.js`/`color-picker.js`'s batches fixed their own instances. `CLICK_ACTIONS`/`FIELD_ACTIONS` typed with the established `Record<string, (ds: DOMStringMap[, el]) => void>` dispatch pattern plus `||''` guards on required-string dataset reads, capturing `const action=el&&el.dataset.action` before indexing (avoids TS2538).
  - `sw.js` `CACHE_NAME` bumped `v109`→`v110` (real logic, bundle hash moved: `app-DYQ1J3kB.js`→`app-cW8vNt_h.js`).
  - **`js/finance.js` is individually strict-clean but not added to `tsconfig.strict.json`'s `include`** — same cluster reasoning as the whole series (imports `analytics-csv.js`/`calendar.js`/`color-picker.js`/`core.js`/`firebase-sync.js`/`settings-managers.js`/`tx-validation.js`/`ui-widgets.js`, several of which are themselves still non-strict). Remaining cluster, finance.js now removed: `settings-managers.js` (122, largest), `goals-profile.js` (53), `auth.js` (51), `app-init.js` (20, disqualified as a natural next pick — it's the central orchestrator importing nearly the whole graph, same reasoning that ruled it out in earlier batches).
  - Verified: `npm run build`/`lint`/`typecheck` (all 4 configs) clean, plus `tests/unit.mjs`, `smoke.mjs`, `e2e-crud.mjs`, `csv-import-export.mjs`, `ai-category-suggestion.mjs`, `receipt-scan-ui.mjs`, `tx-swipe-delete.mjs`, `e2e-modals.mjs` — all green, covering every real code path this batch touched (transaction CRUD, CSV round-trip, AI category suggestion, receipt OCR, swipe-delete, tag/rule manager modals).

### Preact adoption (proof of concept)
Third piece of the "doing this properly" initiative, deliberately scoped to one proof-of-concept widget, not a rewrite (confirmed with the account owner before starting). **Preact** (~4KB gzip core), plain hyperscript `h()` — no JSX, no build-time transform.
- **Target: the Debt tab's payoff-forecast widget** (`renderDebtForecast()`/`DebtBurndownChart`/`DebtForecastText` in `js/debt.js`) — pure, read-only, one call site, previously hand-rolled `innerHTML=` template concatenation. No other widget touched.
- **Vendored under `js/vendor/preact/preact.module.js`, not a bare npm import** — a real architectural constraint: GitHub Pages serves `js/debt.js` unbundled, where a bare package specifier isn't a valid URL and throws (all-or-nothing static import failure). Vendoring resolves identically on both deploy targets — GitHub Pages loads it as a normal same-origin module, Vite inlines it into the bundle normally (a genuine static import, not `@vite-ignore`d). No CSP change needed (self-hosted, same-origin).
- **`scripts/build-site.mjs` excludes `js/vendor/preact/**` from `dist/` and strips its `STATIC_ASSETS` line** (`ALWAYS_BUNDLED_VENDOR_DIRS`) — unlike Tesseract, Preact is always fully inlined into the bundle, so a standalone `dist/` copy would be dead weight and, worse, would make the dist SW's `cache.addAll()` 404. If a future always-statically-imported vendor file is added, add its directory to this same set.
- **Every code path touching a Preact-managed container must go through `render(h(...), targetEl)`, never a raw `.innerHTML=`** — mixing manual DOM writes with Preact-managed content desyncs Preact's diff tracking. `renderDebtForecast()`'s two text-only early-return paths go through `DebtForecastText` for exactly this reason.
- Followed since by 3 more Preact-rendered widgets (analytics donut chart, FX rates widget, crypto-top sparkline — see Finance-tab widgets below), same small stateless-`h()`-tree pattern each time. Expanding Preact further into the app is an explicit follow-up conversation, not decided by this proof of concept.

### Firebase data model
- Firestore layout: `users/{uid}/max_tracker/{doc}` where `{doc}` is one of `shifts`, `finance`, `debt`, `backup_v2`, `profiles_meta` (see Multiple profiles below). `DCOL = 'max_tracker'`; `userDoc(name)` builds the path. **`firestore.rules` matches a single path segment** (`match /users/{uid}/max_tracker/{document}`, not `{document=**}`) and requires `document` to match `^(shifts|finance|debt|backup_v2)(@[A-Za-z0-9_]+)?$|^profiles_meta$`, plus shallow validation (top-level key count, `updatedAt`/`at` must be numbers if present). **Adding a new doc *base name* needs a matching `firestore.rules` change**; a new `@<profileId>`-suffixed variant of an existing base name needs none. Run `tests/firestore-rules.mjs` (real emulator, not a read-through) before touching this file. A legacy pre-auth shared `max_tracker/*` collection is explicitly denied — do not resurrect it.
- **Transactions live in their own subcollection, not the `finance` doc**: `users/{uid}/max_tracker/{financeDocName}/transactions/{txId}` (`txId` = `String(tx.id)`). See `MIGRATION_PLAN_transactions.md` for rationale (1 MiB doc-size risk, whole-doc rewrite on every save) — completed as a direct cutover since the app was pre-launch with no real users to protect through a phased rollout. All access goes through `js/firebase-sync.js`'s TRANSACTIONS SUBCOLLECTION helpers (`txCollection()`, `saveTransactionDoc()`, `deleteTransactionDoc()`, `loadTransactionsFromSubcollection()`, `batchWriteTransactions()`, `batchDeleteTransactionsByIds()`, `deleteAllTransactionDocs()`) — never `scheduleSave()` for transaction writes. `fbLoadNow()` does a one-time client-side migration per account/profile from the legacy `finance.data` array, gated by `AppState.txMigrated`/`finance.txMigrated` so an empty subcollection after all transactions are deleted never re-migrates. Account deletion explicitly deletes every subcollection doc too (Firestore doesn't cascade-delete). `functions/lib/sweep.js` reads the subcollection via Admin SDK, falling back to `finance.data` only when empty (pre-migration account or race). `scripts/backfill-transactions.mjs` is an optional one-time bulk-migration operator script, superseded by the automatic client-side migration.
- **CSV import** (`js/analytics-csv.js`): round-trips `exportTransactionsCSV()`'s own dialect only — no generic bank-statement mapping UI. `parseCSV()` handles `csvEscape()`'s exact dialect (`;` delimiter, `"`-quoted, BOM). `buildTransactionsFromCSV()` matches columns **positionally**, never by header text (labels are localized). Wallet **name** resolved back to id via `AppState.wallets.find`; each row revalidated through `validateTransactionDraft()`; a failing row is skipped and counted, not fatal. **No id-based dedup** (the export format has no id column) — re-importing the same file twice creates duplicates, a documented limitation surfaced in the confirm dialog. Covered by `tests/csv-import-export.mjs`.
- The `finance` doc still holds everything else: wallets, categories, budgets, subcategories, currencyRates, tags, autoRules, recurring, `shoppingList`, goals, profile, subscription, widgets, `integrations` (Monobank, see below), and `notifSettings` (including `notifSettings.timeZone`, an IANA zone name for the push sweep's local-time calculations).
- **`renderFinance()`'s transaction list does a targeted DOM update, not a full rebuild** — matches existing `.tx-item` nodes by `data-id` against the new `visible` list, regenerating inner content in place via `txItemInnerHtml()` but keeping the same outer element (so listeners stay bound and the entrance animation doesn't replay on unrelated re-renders); genuinely new transactions get a fresh node; reordered nodes get `insertBefore`; removed transactions' nodes are removed. Transitioning from the empty-state/skeleton markup needs an explicit `:not(.tx-item):not(.tx-view-all-btn)` cleanup pass first, since the diff only tracks those two selectors. Covered by `tests/tx-list-targeted-update.mjs`.
- `push_tokens/{uid}` (separate top-level collection) holds `{token, updatedAt, sentDaily, sentBudget, sentRecurring, profileState}` for the FCM sweep; rule scoped to `request.auth.uid == uid` plus type/size checks on client-writable fields (not a full key allowlist, since the Admin SDK writes its own dedup fields bypassing rules).
- `error_reports/{uid}` (separate top-level collection) mirrors `js/core.js`'s `logAppError()`'s capped 20-entry local error log (`localStorage` `mx_errlog_<uid>`), best-effort synced (rate-limited to once/30s), stack traces truncated to 500 chars. Only fires when `AppState.currentUser` is set. The client never reads this collection — the account owner checks via the Firebase console. `firestore.rules` gates writes only (owner-only, ≤20-item `entries`, numeric `updatedAt`). Not cleaned up on account deletion — small, capped, harmless-if-orphaned, same as `push_tokens`.
- **Empty `catch(e){}` convention**: a *new* catch block only stays silently empty if it's genuinely one of four safe categories — (1) `localStorage`/`sessionStorage` read/write, (2) `setPointerCapture` in swipe handlers, (3) `Intl.DateTimeFormat()`/`document.referrer`/`matchMedia()` reads with an already-safe fallback, (4) `navigator.vibrate()` already guarded by a feature check. Anything that could represent a real, unexpected failure should call `logAppError(kind, detail)` (`js/core.js`) instead.
- Client-side caching/sync: every piece of state mirrors into `localStorage` via `lsKey(name)` → `mx_<name>_<uid>` (loaded first for instant paint), reconciled against Firestore in `fbLoadNow()`. Writes go through `scheduleSave()` (debounced) → `fbSaveNow()`, optimistic concurrency (`lastKnownUpdatedAt` per doc) prompting on conflict. A handful of device-global (not per-user) keys — theme, language, hide-amounts, exchange-rate source/timestamp, local-notification dedup — live under a separate `mx<Name>` naming (`mxTheme`, `mxLang`, `mxNotif`) rather than `lsKey()`, since they apply pre-auth or aren't per-account. `mx*` is the current prefix (renamed from a legacy `xamss*` prefix while pre-launch) — don't reintroduce the old one. It's decoupled from the marketing brand name on purpose, so it didn't need to change again on the Zminka→Rytm rename.

### Multiple profiles per account
One Firebase Auth account can hold several independent data-profiles (e.g. "Я"/"Дружина"), switchable in Settings → Профілі.
- `activeProfileId` (module-level, default `'default'`) drives both `userDoc(name)` and `lsKey(name)`: default profile → unsuffixed doc names/keys (zero migration); any other profile → `"@<profileId>"` suffix on both. Every save/load path funnels through these two functions, so nothing else needed to change.
- `profiles_meta` (account-wide, never suffixed) holds `{list:[{id, name, avatar, createdAt}], updatedAt}` — labels/avatars only; each profile's real identity (nickname/avatar) lives in *that profile's own* `finance` doc.
- `activeProfileId` itself is a **per-device** choice (`localStorage['mx_activeProfile_'+uid]`), not synced.
- `switchProfile(id)` flushes the current profile (`fbSaveNow()`) before reassigning and calling `fbLoadNow()` again, reusing all seeding/rendering logic.
- The Cloud Function sweeps every profile in `profiles_meta` independently. Per-profile dedup state lives in `push_tokens/{uid}.profileState.<profileId>`; pre-existing flat fields are read as the default profile's initial state until the sweep writes `profileState.default` for the first time.
- **Any render function that bails early on an empty collection must explicitly clear its previously-rendered DOM first** — otherwise switching to a profile with no data leaves the previous profile's numbers on screen (bit us once with the Debt tab).

### Shared profiles
A second Firebase Auth account can be invited to read/write one of this account's own profiles (shifts/finance/debt + transactions) — built for a couple sharing one household's finances, not a general multi-tenant model. Verified against a real local emulator (`tests/firestore-rules.mjs`) before any client code was written.

- **Reuses the existing `users/{uid}/max_tracker/{doc}@{profileId}` tree** — no parallel `shared_profiles` collection; the owner's side behaves exactly like any other profile. The only client-side generalization is in `js/firebase-sync.js`: `userDoc(name)` resolves against `AppState.activeProfileOwnerUid || AppState.currentUser.uid` (never for `profiles_meta`), and `lsKey(name)` returns `null` (no local caching) whenever `activeProfileOwnerUid` is set — a shared profile always loads fresh from Firestore.
- **Membership lives in a new doc-name kind, `shared_members`** (suffixed the same as shifts/finance/debt), holding `{members:[ownerUid, ...], updatedAt}` under the **owner's own** tree.
- **Joining is invite-code based, never open self-join.** `profile_invites/{code}` (new top-level collection — a joiner has no relationship to the owner's account yet) holds `{ownerUid, profileId, profileName, createdBy, createdAt, expiresAt, usedBy}`. 8-char unambiguous-alphabet code, client-generated, 24h TTL, single-use. **Redeeming is a genuine two-step, rules-enforced sequence**: step 1 marks the invite used (`usedBy: null → joinerUid`); only after that commits does step 2 (joining `shared_members@{profileId}`) become possible, because that write's rule does a `get()` on the invite doc and requires `usedBy == request.auth.uid`. A single-write "just check the members array shape" rule would leave a real hole — self-add to `shared_members` for any guessed `{ownerUid, profileId}` pair with no real invite, since Firestore rules can't cross-check "a different document was also written" any other way. Two joiners racing the same code can't both win — commits are serialized per document.
- **`firestore.rules`' main block gained**: `isSharedMember(ownerUid, document)` helper; read/write grant for `shifts`/`finance`/`debt` (never `backup_v2`/`profiles_meta`/`shared_members` itself) to any member; join/leave branches for `shared_members`. **Deletion of the profile's data docs stays owner-only even when shared** — a member can edit or leave, never delete the doc or forcibly remove another member (`removeAll` restricted to your own uid). The transactions subcollection got the identical extension.
- **Client functions** (`js/firebase-sync.js`): `shareCurrentProfile()`, `redeemSharedInvite(code)` (returns `{ok:false, reason}` rather than throwing), `leaveSharedProfile(ownerUid, profileId)`. UI: Settings → Профілі "Поділитись поточним профілем" (plain `uiAlert` with the code) and "Приєднатися за кодом" (`uiPrompt`); each shared profile row shows a badge and leave button.
- **Still out of scope, disclosed**: `notifSettings` lives inside the shared `finance` doc, so it's effectively shared too (a member changing the reminder time changes it for the owner). No cleanup of `shared_members`/`profile_invites` on account deletion. Granular permissions and push support (both listed here as v1 gaps) were implemented later — see the two sections below.

### Granular permissions on shared profiles
Two roles: **editor** (default, full read/write) and **viewer** (read-only).
- **`shared_members` gained a `roles` map**: `{members:[...], roles:{uid:'editor'|'viewer'}, updatedAt}`. A member absent from `roles` defaults to `'editor'` — backward-compatible, no migration, no existing shared profile silently loses write access.
- **Enforcement is in `firestore.rules`, not the client** — `memberRole()`/`isEditorMember()` gate `create`/`update`/`delete` on the main doc and the transactions subcollection; `isSharedMember()` alone (any role) still gates `read`. A viewer's client also gets read access to `shared_members` itself, to look up its own role. `canEditActiveProfile()` (`js/core.js`) is a client-side UX convenience mirroring this, never the security boundary.
- **Role changes are owner-only, applied after the fact, never during redemption** — every new member always joins as editor; the owner downgrades afterward via the **"Учасники" (Members) manager** (`js/color-picker.js`'s `openSharedMembersManagerUI()`/`renderSharedMembersList()`/`toggleMemberRoleUI()`, `#shared-members-modal`), a button on each owner-side profile row.
- **`AppState.activeProfileRole`** (`js/state.js`, default `'editor'`), loaded via `loadActiveProfileRole()` on `switchProfile()` and at cold-init. `canEditActiveProfile()` guards `addTransaction()`/`deleteTransaction()`, `saveModalSelection()`, `addDebtEntry()`/`deleteDebtEntry()` with a `shared_profile_readonly` toast instead of a doomed write. `applyProfileReadOnlyUI()` (`js/ui-widgets.js`) toggles `body.profile-readonly`, hiding `.fin-fab`. **Deliberately not individually guarded**: secondary settings mutations (renaming a wallet, editing a budget/category/tag/rule) — a viewer attempting one still fails safely via `firestore.rules`, just with the generic `sync_autosave_error` toast.

### Push notification support for shared profiles
`functions/lib/sweep.js` resolves a `dataOwnerByProfile` map before fetching each profile's docs: `profiles_meta.list` carries `{id, name, kind:'shared', ownerUid}` for a shared-profile reference, and `dataOwnerFor()` returns that `ownerUid` (or the token holder's own uid otherwise) — every `users/${uid}/...` path in the "other profiles" fetch uses this instead of always the token holder's own uid. `sweepProfile()` takes an optional `dataOwnerUid` parameter (default `= uid`). The Admin SDK bypasses `firestore.rules` entirely, so this needed no rules change — only which path is read. A member's push works the same regardless of viewer/editor role. **`notifSettings` is still the owner's** — a member's push uses whatever reminder time/toggles the owner last saved, a disclosed quirk (see Shared profiles above).

### Monobank integration
"Прив'язати Monobank" (Settings → Фінанси) — pulls transactions from the account owner's Monobank cards/jars via Monobank's Open API (`api.monobank.ua`). Scope decided upfront: **manual "Синхронізувати" button, not a webhook**, and **links every card and jar on the token at once**, not a per-account picker.

- **Cloud Function proxy needed because `api.monobank.ua` never sends CORS headers** (same root cause as `PRIVAT_RATES_URL`). `functions/index.js`'s `monobankProxy` (`onRequest`, exposed same-origin via the `"/api/monobank"` Hosting rewrite) relays `action=client-info` and `action=statement&account=&from=&to=`, attaching the caller-supplied `X-Monobank-Token` header. **Unlike `privatRates` (public, no auth), this proxy requires a valid Firebase ID token** (`Authorization: Bearer <idToken>`, verified via `firebase-admin`) — otherwise it'd be a free anonymous relay riding on this project's quota. Query-building/window-validation logic is split into `functions/lib/monobank.js` for unit-testability (`tests/monobank-proxy.mjs`).
- **The token is a live bank credential**, stored in `AppState.integrations.monobank = {token, clientName, accounts:[{id, kind:'account'|'jar', label, currencyAlpha}], mapping:{monobankAccountId: walletId}, lastSyncAt}` inside the existing `finance` doc — same per-uid rules protection as every other field, no new doc kind/rules change. Only ever sent to this app's own `monobankProxy`.
- **Connecting** (`connectMonobankUI()`, `js/monobank.js`) calls `client-info` once, auto-creates one wallet per account/jar (`createWalletForMonobankAccount()`), records `monobankAccountId → walletId` in `mapping`. No manual per-account picker.
- **Syncing** (`syncMonobankUI()`) walks `mapping`, calling `statement` for `[lastSyncAt ?? now-31days, now)`, chunked into ≤31-day-+1h windows (Monobank's hard limit), capped at 14 chunks as a defensive backstop. **Every call is paced at least 61s apart** (`paceMonobankRequest()`, `SYNC_REQUEST_GAP_MS`) — Monobank enforces "1 request/60s per token" across the whole API, so a multi-account sync can take minutes; `#monobank-sync-status` shows live progress text. `hold:true` entries (pending preauth) are skipped. **Dedup is by Monobank's own stable per-entry `id`**, stored as `monobankId` on the created transaction — re-syncing an overlapping window never creates duplicates.
- **Category**: existing keyword auto-rules (`findMatchingRule()`) get first say; unmatched falls back to `"Інше"`. No MCC→category mapping. **No transfer detection** — an internal move between two Monobank accounts shows as separate expense+income, same simplification as CSV import.
- **Testing**: `tests/monobank-proxy.mjs` (plain node — URL/window validation) and `tests/monobank-connect.mjs` (stubbed-Firebase Playwright + a `page.route()` stub for `/api/monobank`, since there's no Functions emulator in this test setup). `setMonobankSyncGapMsForTesting()` lets the test skip the real 61s pacing gap.
- **CSP**: none needed — client only calls same-origin `/api/monobank` (already covered by `connect-src 'self'`).

### Firebase App Check
Provisioned, but **not initialized in the client and not enforced**.
- Uses **reCAPTCHA Enterprise**, not classic reCAPTCHA v3 — classic v3 site keys can only be created through a human-driven web form with no REST API, so an agent session could never provision one unattended; reCAPTCHA Enterprise is a real GCP resource (`recaptchaenterprise.googleapis.com`), creatable by any principal with the right IAM role (`roles/recaptchaenterprise.admin` + `roles/firebaseappcheck.admin`, both self-granted and kept permanently — see Operating conventions). Functionally equivalent (score-based bot detection); its own free tier (1M assessments/month) is well beyond this app's traffic.
- Key: `projects/311094677098/keys/6LdDGlctAAAAAB3lX0HQS9ao_B4Bn8w0O_KFJDYk`, `integrationType: SCORE`, `allowedDomains: [maxtr-c238f.web.app, maxtr-c238f.firebaseapp.com, sudak666.github.io]`.
- **Current client state**: `js/core.js` deliberately does **not** import or call `initializeAppCheck()`. Firebase Auth phone/SMS verification can also enter project-level reCAPTCHA Enterprise AUDIT mode; in production the Auth-managed Enterprise site key failed before the SDK reached its v2 fallback (`Invalid site key or not loaded in api.js`). `js/auth.js` therefore seeds Auth's internal recaptcha config as disabled immediately before phone send/link flows so those flows use the explicit `RecaptchaVerifier` v2 path that worked before Enterprise/AUDIT was enabled. `core.js` keeps `export const appCheck = null` as a placeholder so a future App Check re-enable is explicit and tested against phone auth.
- **CSP**: `script-src` still includes `https://www.google.com` for Firebase Auth's phone reCAPTCHA. `connect-src`/`frame-src` already cover the existing phone-auth reCAPTCHA setup.
- **`sw.js`**: `firebase-app-check.js` is intentionally not listed in `FIREBASE_SDK_ASSETS` while the client does not import it; `CACHE_NAME` must still be bumped whenever this list or another precached asset changes.
- **If another Firebase SDK module import is ever added to `js/core.js` (or any `js/*.js` file), grep every `tests/*.mjs` file for `firebase-app.js`** — every stubbed-Firebase Playwright test needs a matching stub route for a new SDK import or the whole module graph fails to load and every test hangs on a blank lock-screen (this happened when App Check was added — an inert `STUB_APP_CHECK` had to be added to all affected files).
- **Deliberately NOT enforced**: both `firestore.googleapis.com` and `identitytoolkit.googleapis.com` (Auth) are `UNENFORCED` (the default) in the App Check Management API. Flipping enforcement while the client is not initializing App Check would lock real clients out. If asked to re-enable App Check, first solve/test the phone-auth reCAPTCHA interaction, then initialize App Check, monitor verification rate, and only then consider enforcement in Firebase Console.

### Auth
Three sign-in methods: email+password, Google popup, phone number (SMS via `signInWithPhoneNumber` + invisible `RecaptchaVerifier`). Layered on top, independent of Firebase Auth: a local PIN (SHA-256 hash in localStorage, `mx_pin_<uid>`) and optional WebAuthn biometric unlock — gate the UI locally, never replace the Firebase session.

Settings → Акаунт lets an already-signed-in user **link/unlink a phone number** (`linkWithPhoneNumber`/`unlink(currentUser,'phone')`) — separate flow with its own `link-recaptcha-container`/`RecaptchaVerifier` instance.

**reCAPTCHA retry gotcha**: `RecaptchaVerifier.clear()` can itself throw partway through, leaving the widget's DOM node behind — the next `RecaptchaVerifier.render()` then throws `"reCAPTCHA has already been rendered in this element"`. Both phone flows force the container's `innerHTML` empty on error (`resetRecaptchaContainer(containerId)`) regardless of whether `.clear()` succeeded — do this for any new reCAPTCHA-backed retry flow too.

**Phone auth troubleshooting** (not code issues, easy to mistake for one): (1) needs the **Blaze** plan; (2) needs **Authentication → Settings → SMS region policy** to explicitly allow the tested country; (3) **Brave disables Web Push by default** (`brave://settings/privacy`) — causes `AbortError: Registration failed - push service error`, not a code bug; (4) **GitHub Pages isn't auto-authorized** — `sudak666.github.io` isn't in Firebase's default Authorized domains (`*.web.app`/`*.firebaseapp.com` are), so invisible reCAPTCHA fails there while working on `.web.app`; add it manually (SETUP.md §2). `authErrorMessage()` in `index.html` maps `auth/captcha-check-failed`/`auth/unauthorized-domain`/`auth/network-request-failed` to specific messages; (5) **`auth/error-code:-39` (503 on `sendVerificationCode`) for every number** is Firebase's automated anti-toll-fraud system temporarily blocking the carrier/region, not billing/API-key/region-policy/a specific number — confirmed with Firebase Support (case 10414309). `authErrorMessage()` maps this and `auth/internal-error` to a dedicated message (`isPhoneFlow`-scoped). Real fix: enable reCAPTCHA SMS Defense (needs Web SDK 11+, why the Firebase JS SDK is pinned to `12.11.0`) — see SETUP.md §5.

### Decorative glow/gradient audit finding
An audit flagged glow/gradient CSS as reading closer to a 2023-2024 saturated-glow aesthetic than 2026's calmer trend. Investigation found `index.html`'s `:root` already carries a deliberately-tuned 3-tier surface system (`--shadow-lock-logo`/`--shadow-fab`/`--donut-glow`/etc.), including a separate prior light-theme retune — glow intensity here is iterated-on, not an oversight. Only the one concrete complaint that survived scrutiny was fixed: `--tab-glow-ripple-alpha` (the transient tap ripple, not the persistent branding glow) trimmed `.42`→`.34` dark / `.24`→`.19` light; everything else deliberately untouched. A broader visual-direction change (glassmorphism, flatter cards) would need the account owner's explicit sign-off and live screenshot feedback — a genuine taste call, not something to ship unilaterally from an audit checklist.

### On-device AI category suggestion
`js/finance.js`'s `maybeSuggestCategoryWithAI()`, wired to the `#fin-comment` input listener alongside `applyAutoRuleToForm()`. Uses Chrome's built-in **Prompt API** (`self.LanguageModel`, stable from Chrome 138) — runs on-device, no network call, no API key, comment text never leaves the browser. Feature-detected, total no-op everywhere unsupported (including this repo's own CI Chromium). `tests/ai-category-suggestion.mjs` stubs `self.LanguageModel` via `page.addInitScript` to prove the real code path, not just the no-op fallback.
- **Keyword rules always win** — `findMatchingRule()` checked first; AI only runs when nothing matched.
- **Debounced (700ms) and staleness-checked at every await boundary** (`stillFresh()`) — a suggestion computed for text the user has since changed/cleared/overridden is discarded.
- **Only applied if it exactly matches (case-insensitively) one of the finance type's real category names** — never lets the model invent a category string.
- **Comments under 8 characters never trigger a call** — short notes are already well handled by keyword rules.
- On a match: same UX as `applyAutoRuleToForm()` (sets `<select>`, dispatches `change`, `csSync()`s, `sparkle` toast) but with its own `ai_category_suggested` i18n string.

### Receipt scanning (OCR)
"Сканувати чек" button on the new-transaction form (`#receipt-scan-group`/`#fin-scan-receipt-btn`, hidden for transfers). Picks/photographs an image (native camera on mobile via `capture=environment`) and runs **Tesseract.js** (WASM, on-device OCR) to prefill amount + date — never blocks the form; an unreadable photo leaves fields as they were, with a toast.
- **Self-hosted, not CDN-loaded — a hard CSP requirement.** Tesseract.js's defaults pull from `cdn.jsdelivr.net`, but this app's `script-src` has no CDN allowance. Vendored into `js/vendor/tesseract/` (~9MB: `tesseract.esm.min.js`, `worker.min.js`, `tesseract-core-lstm.wasm.js` — LSTM-only, non-SIMD for universal compat — plus `lang-data/{ukr,eng}.traineddata.gz`), pulled via `npm pack` (registry.npmjs.org is reachable from this sandbox; jsdelivr/unpkg are not). **The separate `.wasm` binary file is not needed** — `tesseract-core-lstm.wasm.js` already embeds it inline as base64; confirmed via network-request logging during a real E2E run, then deleted (cut ~12MB→~9MB vendored footprint).
- **`corePath`/`workerPath`/`langPath` point at exact same-origin files, and `workerBlobURL:false` is required** — Tesseract.js's browser default spawns its worker via a `Blob`+`URL.createObjectURL()`, which this app's `worker-src 'self'` (no `blob:`) blocks; `workerBlobURL:false` forces the direct same-origin-URL path instead. Passing `corePath` as an exact `.js` file also sidesteps Tesseract.js's own SIMD-feature-detection branching, avoiding the need to vendor both variants.
- **CSP needed one addition: `'wasm-unsafe-eval'` on `script-src`** — `WebAssembly.instantiate()` is blocked without it (narrower than `'unsafe-eval'`, doesn't enable `eval()`/`new Function()`). No other CSP directive needed — image data never touches `img-src`/`connect-src`/blob URLs (read via `FileReader.readAsArrayBuffer()` internally).
- **Two different base paths for the same directory, on purpose**: the dynamic `import()` specifier (`./vendor/tesseract/...`) resolves relative to the importing module's own URL; the plain path strings Tesseract.js's `createWorker()` takes (`workerPath`/`corePath`/`langPath`) are resolved by the library relative to the page's URL (`/` on Firebase Hosting, `/Maxtr/` on GitHub Pages), so those need `./js/vendor/tesseract/...`. Mixing the two 404s silently.
- **Amount-extraction gotcha**: the fallback ("largest number in the text," when no total/сума keyword line matches) used to lose to a receipt's own date (`16.07.2026` contains a bare `2026`, numerically larger than the real total). `parseReceiptText()` masks the matched date span out before amount extraction, and prefers decimal-formatted numbers (`X.XX`) over bare integers. Locked in as a regression case in `tests/receipt-ocr-parse.mjs`.
- **Real phone photos (3000-4000px, several MB) can hang the non-SIMD WASM core with no JS-catchable error, and a long real scan has no UI feedback once the toast auto-hides** — both fixed: `downscaleImage()` (createImageBitmap + canvas, capped 1800px long side, JPEG) runs before every `recognize()`; `handleReceiptFile()` swaps the button's own label to a persistent "processing" state for the scan's duration; a hard 45s timeout (`withTimeout()`) rejects a genuinely stuck call, re-enabling the button with a `receipt_scan_timeout` toast. `scanReceiptImage()` takes an optional `timeoutMs` override for fast test verification (unused by the real call site).
- **CI/local test stubs the dynamic `import()` boundary via `page.route()`** rather than running real WASM OCR. **Needs `browser.newContext({serviceWorkers:'block'})`, not plain `newPage()`** — `sw.js`'s `clients.claim()` can claim the page before this test's late dynamic import fires, so its `fetch` handler intercepts the request before Playwright's route ever sees it (silently loading the real ~9MB library) — worth remembering for any future test whose stubbed fetch happens late in page load, not in the first moment.
- `js/vendor/tesseract/*` is **not** in `sw.js`'s `STATIC_ASSETS` (only reached via a dynamic `import()` inside a function body — eager-precaching ~9MB for a rarely-used feature would be wasteful); it's cached opportunistically on first real use. `js/receipt-ocr.js` itself (the wrapper module, statically imported) **is** in `STATIC_ASSETS`.
- Apache-2.0 licensed — `LICENSE-tesseract.js.md`/`LICENSE-tesseract.js-core.txt` kept alongside the vendored files.

### Premium / free-tier limits
`subscription = {plan, expiresAt}` lives in the `finance` doc. `FREE_LIMITS`, `canAddMore(kind, count)`, `showPremiumUpsell(kind)` still exist, but **no "add" action currently calls `canAddMore`** — caps were removed since, with no payment provider, a hard cap was a dead end for every user. If a payment provider gets wired up, re-add the `if(!canAddMore(...)){ showPremiumUpsell(...); return; }` guard at the top of each `add*()` function (see git history around "Remove remaining free-tier caps") rather than reinventing the gating pattern.

### Finance-tab widgets
`widgets = {rates, converter, analytics, chart, goals}` (synced in `finance`) — only `goals` is still a real toggleable widget in `WIDGET_ORDER_DEFAULT`/`WIDGET_SECTION_IDS` (`js/state.js`/`js/core.js`) and `WIDGET_DEFS` (`js/settings-managers.js`); `rates`/`converter`/`analytics`/`chart` all moved into `#tools-modal` (opened via "Інструменти"), always reachable there rather than conditionally visible in the main scroll — the Finance tab scroll is now balance+quick-actions+transaction-list+FAB+goals(if any)+budgets(if any). `AppState.widgets.rates`/`.converter`/`.analytics`/`.chart` remain as harmless unused fields (existing users' Firestore docs already carry them). `applyWidgetVisibility()` is the single reconciliation point — calls `renderGoals(); renderDailyTip(); renderCryptoTop(); applyWidgetOrder();`. Adding a new toggleable widget means updating the default in 3 places (grep `widgets.goals`) plus `WIDGET_ORDER_DEFAULT`/`WIDGET_SECTION_IDS`/`WIDGET_DEFS`. `TX_LIST_COLLAPSED_COUNT` (`js/analytics-csv.js`) is 5.
- **"Порада дня"** (`dailyTip`, `js/dashboard-widgets.js`) — ~20 local Ukrainian finance tips, rotated by calendar day, zero network dependency. **"Топ криптовалюти"** (`cryptoTop`) — BTC/ETH price + 24h change + 7-day Preact sparkline, fetched from CoinGecko's public `/coins/markets` (CORS-friendly, no proxy needed; `connect-src` gained `https://api.coingecko.com`). Cached in device-global `mxCryptoTopCache`, refetched at most once/30min (`maybeRefreshCryptoTop()`), self-hides until first successful fetch. `.crypto-top-price` is deliberately excluded from `body.amounts-hidden`'s blur list — public market data, not personal. Covered by `tests/dashboard-widgets.mjs`; `sw.js`'s `STATIC_ASSETS` and `scripts/build-site.mjs`'s `MODULE_GRAPH_FILES` both include `dashboard-widgets.js`.

### Notifications (two independent delivery paths, same three conditions)
Both paths check: daily "did you log anything today" reminder, budget-exceeded, upcoming-recurring-payment.
1. **Local** (`NOTIFICATIONS` section): fires only while the app is open, via the `Notification` API, checked on load/visibility-change/a 5-minute interval. Dedup via localStorage keys.
2. **Push** (`PUSH NOTIFICATIONS` + `functions/index.js`): client registers an FCM token (`push_tokens/{uid}`) via `getToken()` + `sw.js`. A scheduled Cloud Function (`notificationSweep`, hourly) re-implements the same three checks server-side and sends via the Admin SDK, with its own dedup fields on the token doc. **If you change the local-notification logic, check whether `functions/index.js` needs the equivalent change** — intentionally kept in sync but separate implementations.

`sendPush(token, title, body, type)` (`functions/index.js`) takes a `type` (`'daily'|'budget'|'recurring'|'debt'`) and looks it up in `NOTIF_ICONS` to set `webpush.notification.icon`. `sw.js`'s `onBackgroundMessage` reads `payload.notification.icon`, falling back to `icon-192.png`. The 4 icon PNGs (`notif-icon-{daily,budget,recurring,debt}.png`, repo root) are hand-generated (`cairosvg`, purple-gradient rounded-square background, white stroke-based glyphs matching `ICON_PATHS`' style — bell/warning/repeat/bank — supersampled 4× then Pillow `LANCZOS`-downscaled for smooth edges, plus a subtle `feDropShadow` on the glyph layer), same-origin so listed in `sw.js`'s `STATIC_ASSETS`. Covered by `tests/functions-sweep.mjs`'s `type='...'` assertions; the display side (`sw.js`) isn't automated — needs a real FCM background delivery, verify manually via a real device if this needs re-checking.

**`badge` (the small Android notification-shade glyph) is a separate image from `icon`, and needs a real alpha channel** — Android renders `badge` using *only* its alpha channel as a white silhouette; a fully opaque RGB PNG (like `icon-192.png`) renders as one solid white blob. `badge-96.png` (repo root, `cairosvg`-rendered from the same `bell` path data at 96×96, no drop shadow — it must stay a pure alpha-channel silhouette for Android's masking to work) is the correct asset. **If a new notification call site is ever added, give it `badge:'badge-96.png'`, never `icon-192.png` or any other opaque image.** The `maxtr-c238f.web.app` origin text shown next to "Rytm" in the notification header is Chrome/Android's own trust-indicator for web push and **not suppressible from app code** — goes away only once this ships as a real installed Android app via a published TWA. `sw.js` `CACHE_NAME` is currently `rytm-v98` — bump it whenever a same-origin precached asset (icon, manifest, unbundled `js/*.js`, `index.html`, etc.) changes, since browsers detect a new SW purely by byte-comparing the script. This applies even when `npm run build` will generate a hashed `dist/sw.js`: the checked-in root `sw.js` is still what GitHub Pages / already-installed PWA mirrors use, and forgetting this was explicitly called out again after the mobile icon/typography changes.

### UI conventions worth following
- Custom-styled `<select>`/`<input type=date>`: call `enhanceSelect()` / `enhanceDateInput()` on it (or add it before `enhanceAllSelects()` runs) — don't hand-roll another dropdown.
- **Never use `<input type=color>`** — opens an unstylable native OS popup. Use the shared `openColorPicker(kind, id)` / `color-pick-modal` pattern instead — wire a new color-editable list by adding a case to `selectPickedColor()` and rendering `<button class="mgr-color" style="background:${color}" onclick="openColorPicker('kind','${id}')">`.
- No native `title="..."` tooltips or other unstylable browser affordances — prefer a visible label/subtitle in the UI, like the rest of the settings rows.
- **"Settings manager" modals** (wallets, categories, budgets, goals, recurring, tags, rules, rates, widgets, pin, premium, profiles, link-phone, avatar/color pickers) share one pattern: `.modal-overlay`+`.modal-card`, an `open*Manager()`-style function, and the shared `closeManagers()` that hides all of them by id — add new manager ids to that list or backdrop-click/close-all won't cover them. **Separately, every `.modal-overlay` (23 of them) already gets focus-trap/Escape-to-close/focus-restore for free** via `setupModalAccessibility()` (a `MutationObserver` on `style`, `AppState.openModalStack`) — automatic for any new `.modal-overlay`/`.modal-card` markup, no id-list registration needed, though it still needs `closeManagers()`'s list separately for backdrop-click. Covered by `tests/e2e-modals.mjs`.
- All icons are the custom monochrome SVG set (`window.Icon(name)`/`setIcon`) — no emoji, no icon fonts. Add new glyphs to `ICON_PATHS`. Check for duplicate icons within the same visible list/row-group before reusing a glyph (reusing across contexts for the *same* feature is fine). **Two vendored icon libraries under `docs/` are offline references for drawing new glyphs** — neither is loaded/shipped by the app (`docs/` is excluded from both the Vite build and Firebase Hosting). Confirmed with the account owner as icon-library-only additions, not a Material Design 3 (or any other design-system) adoption — colors/typography/components untouched.
  - **`docs/material-symbols/`** (Google's Material Symbols, Outlined/weight 400) — solid filled shapes. Wrap the path in `<g transform="translate(0,24) scale(0.025)">` (remaps its native `0 -960 960 960` viewBox into this app's uniform 24×24 one) with `fill="currentColor" stroke="none"`, same as `repeat`/`pharmacy`/`car`/`flame`/`handCoin`/`diffArrows` already do. Wins when a bold solid glyph reads more clearly at 17px/34px than a thin stroke can.
  - **`docs/lucide/`** (Lucide, ISC-licensed Feather Icons fork) — already this app's own native format (`viewBox="0 0 24 24"`, `stroke="currentColor"`, no fill): copy the `<path>` element(s) straight into `ICON_PATHS` with **no transform needed**. Wins when the new glyph should match the *rest* of the app's hand-drawn stroke icons stylistically.
  - Each directory's own `README.md` has the full recipe and refresh command (`npm pack @material-symbols/svg-400` / `npm pack lucide-static`, both from registry.npmjs.org).
- **"Hide amounts" is an opt-in CSS class list, not a generic money-detector** — `filter:blur(7px)` on a fixed selector list (grep `body.amounts-hidden` in `index.html`) — **any new money display needs its own dedicated class added to that list, or it silently leaks.** Don't reuse a shared row/field class also used for non-money text (blurring it would hide the non-money part too) — give the money-only span its own class instead. Covered by `tests/hide-amounts.mjs`.
- **Calendar day cells show shifts as compact colored letter tokens** (`.shift-token`), not word-badges — `shiftCode(t)` prefers an explicit `code` field on the shift type (set on built-ins: Д/Д+/Н/Нр/Нп/В) and falls back to a derived first letter. Give a new built-in shift type a `code`.
- **Debt tab has a payoff-forecast card** (`#debt-forecast`, `renderDebtForecast()`): inline-SVG burndown chart + a payment-count-based pace estimate. Shown only with enough signal (startAmount set, ≥2 payments, positive downward trend). Pure-derived, no schema change.
- i18n: `I18N = {uk:{...}, en:{...}}` plus `tr(key)`; static markup uses `data-i18n`/`data-i18n-placeholder`, applied by `translateStaticDOM()`. No `data-i18n-title` — dynamic titles must be set in JS.
- The bottom nav (`.tabs`) has 5 tabs; `.tab-icon-wrap` is 42px (38px ≤600px). A 6th tab needs re-checking fit — no overflow/scroll handling on that bar.
- `sw.js`'s `fetch` handler must bail out (`return`, no `respondWith`) for any cross-origin request — `respondWith(fetch(...))` throws for some no-cors/redirect combinations (broke reCAPTCHA's CSP-report beacon once). Don't reintroduce a `respondWith` path for anything outside `self.location.origin`.

### Mobile UI redesign
A design review plus several rounds of live-phone-screenshot feedback drove a rework of the densest screens (multi-session, July 2026). These patterns are the established convention — follow them for anything new.
- **Hero metric + chip row** (`.hero-metric`/`.hero-balance` + `.chip-stat-row`/`.chip-stat`): one large primary number + a horizontal scrollable strip of small pill chips, replacing equal-weight `.stats-row` stat-card grids. Used on Finance/Shifts/Debt/Shopping. `.stat-card`/`.stats-row` still exist and are correct where weighting really is equal (Analytics tab's 4 metrics) — not deprecated.
- **Compact manager row** (`.budget-row`/`.rate-row`/`.cat-row`): single line, icon badge + ellipsis-truncated name (needs `min-width:0` on *every* ancestor in the flex chain, not just the text element, or a nowrap row can overflow its own card) + right-aligned input/value. Auto-rules and recurring managers use the same collapsed-summary-row-with-pencil-toggle-to-expand shape (`AppState.expandedRuleId`/`.expandedRecurringId`). **`.mgr-num` gotcha**: `flex:1 1 90px;width:90px` inside a `flex-direction:column` parent (`.mgr-field`) applies `flex-basis` to the *vertical* axis, silently making the input 90px tall too (a solid near-black square) — fixed by dropping the `flex` shorthand, keep `width:90px` alone.
- **FAB + bottom-sheet modal** (`.fin-fab` + `#tx-form-modal`): the Finance "Нова операція" form is a modal opened by a floating `+` button, not a permanently-expanded inline form. Apply the same pattern to any future "always-expanded form on a primary tab."
- **PWA shortcuts + Web Share Target**: `manifest.json`'s `shortcuts` (long-press → "Нова операція") and `share_target` (GET-based) both land on `js/app-init.js`'s `handleLaunchParams()`, called last in `init()` (after `fbLoadNow()`). Reads `URLSearchParams`, strips the query via `history.replaceState`, clicks the same `[data-action="open-new-tx-modal"]` FAB. A shared `title`+`text` prefills the comment and, best-effort, the amount (first plain number found). Covered by `tests/pwa-launch-params.mjs`.
- **Swipe-to-delete list rows** (Telegram/Gmail-style): the pattern for any destructive delete action — Finance tx list (`.tx-item`/`setupTxSwipe()`) and Debt payment rows (`.debt-row`/`setupDebtRowSwipe()`), intentionally duplicated per-file rather than shared. Outer element is a `position:relative;overflow:hidden` clipping shell; the visible card is an `*-inner` child that shrinks its own `width` (never `transform:translateX` — clips left-edge content) to reveal a red delete button behind it. **Flexbox gotcha**: a flex item with `overflow` other than `visible` gets an automatic minimum size of 0 — a swipe-clipped row inside a `display:flex;flex-direction:column;overflow-y:auto` list needs an explicit `flex-shrink:0` or it silently crushes to a sliver once content exceeds `max-height` (hit both `.debt-row` and `.tx-item`; also silently broke pull-to-refresh, since a non-overflowing container reads as non-scrollable). Both are covered by geometry regression assertions (`tests/stress-tx-list.mjs`, `tests/debt-swipe-delete.mjs`). **Give any new swipe-clipped row `flex-shrink:0` and its own `border-radius` matching the row** (the outer clip only rounds the far edge for free).
- **Floating pill bottom nav**: `.tabs` is a detached rounded capsule, not edge-flush. `.app`'s bottom padding (112px) and `.fin-fab`'s offset (104px) are tuned to this bar's height+gap — re-check both if the bar changes.
- **Icon-based avatars, not emoji**: `BUILTIN_AVATARS` uses `window.Icon(a.icon)`. Exactly three render call sites (`renderAvatarPicker`, `renderProfileAvatarPickerGrid`, `renderProfilesUI`) plus `fillAvatarEl` — when this array's shape changes, grep `BUILTIN_AVATARS` **and** the field name itself (`.emoji`/`.icon`), not just the array name, to catch every destructured/aliased read.
- **Collapsed-by-default secondary panels**: e.g. Shifts' "Швидке заповнення" quick-fill, collapsed by default (`toggleQuickFill()`). Don't reuse `min-width:0` on a `.cs-sm` custom-select without either a real minimum or a guaranteed-wrapping container — it can shrink below its own text's width instead of wrapping.
- **Haptic feedback**: `showToast(msg, icon)` fires `navigator.vibrate(...)` (feature-detected) automatically — no need to call it separately.
- **Accessibility touch targets**: `.filter-chip`/`.settings-group-chip`/`.debt-chip`/`.subcat-toggle` and `.mgr-color`/`.mgr-del`/`.mgr-edit`/`.debt-chip-del` bumped to clear ~44px (later reconciled against the real WCAG 2.2 AA floor, SC 2.5.8, which is a more lenient 24×24 CSS px — most "left out" items already clear that). `--muted2` contrast raised to `#98979e` dark / `#626269` light (was under 4.5:1 AA for normal text). `.subchip button`'s small "×" got an asymmetric hit-slop pseudo-element instead of a size bump (safe there specifically because of the safe zone on its trailing edge).
- **Font sizes**: every sub-12px `font-size` was raised to 12px, then (separate pass, direct account-owner feedback that content still read small) the whole small-text scale shifted +1px again: `12px→13px`, `13px→14px`, `14px→15px` (and half-steps), `15px`+ untouched. **Current baseline — check the live value in the file, not any older paragraph's px numbers.**
- **Pull-to-refresh**: `setupPullToRefresh()` (`js/app-init.js`) — drag down from `scrollTop===0` reveals `#ptr-indicator`, snaps back under a 70px threshold or triggers the same `fbLoadNow()`+toast the topbar refresh button uses. Relies on `body{overscroll-behavior-y:contain}` to suppress native pull-to-refresh, not JS `preventDefault()` (uses `{passive:true}`). Covered by `tests/pull-to-refresh.mjs`.
- **Sheet-grabber drag-to-dismiss** (`initSheetDrag()`, `js/ui-widgets.js`): `.sheet-grabber` is a full-width 28px-tall hit-box (visible bar is a `::after`), not just the visible bar itself — a narrow hit-box caused inconsistent-feeling drags from the sheet's rounded corners. Also dismisses from within `.modal-card-body` itself for content-heavy sheets: a downward drag only counts as dismiss if the first unambiguous movement (past 8px slop) is downward **and** the body was already at `scrollTop===0` — any other case is left alone with no `preventDefault()`, so normal scrolling is untouched. Covered by `tests/sheet-drag-dismiss.mjs`.
- **Empty-state illustrations**: `emptyStateHtml()` (`js/ui-widgets.js`) wraps the monochrome icon in a larger radial-gradient glow circle + floating accent dots, built from the app's own icon set/palette (no emoji/external artwork), plus a `prefers-reduced-motion`-aware entrance animation. All 5 existing call sites picked this up automatically.

### Android / Google Play packaging (TWA)
Ships as a **Trusted Web Activity (TWA)** — a thin Android wrapper opening the live `maxtr-c238f.web.app` full-screen, not a separate native codebase. No local Android project checked into this repo; generated on demand.
- **Canonical domain is `maxtr-c238f.web.app`** (not GitHub Pages — Play's Digital Asset Links verification needs one fixed origin with no subpath).
- **`.well-known/assetlinks.json`** (repo root) lets Android show the app without a URL bar — declares the package ID + signing cert's SHA-256 fingerprint, verified against `https://maxtr-c238f.web.app/.well-known/assetlinks.json`. `firebase.json`'s hosting `ignore` has an explicit `"!/.well-known/**"` un-ignore (dotfiles are excluded by default) plus a `Content-Type: application/json` override. If the fingerprint and the actual signing key ever drift, the app degrades to a Chrome URL bar rather than failing outright — fix is "update the fingerprint, redeploy hosting."
- **Package ID is `ua.rytm.app`** — freely changeable up until the *first* real Play Store upload (hasn't happened yet); after that, changing it means a new listing, not an update.
- **The actual APK/AAB build cannot happen from a Claude Code cloud sandbox** — this environment's egress proxy 403s `dl.google.com`, `pwabuilder.com`, and `maxtr-c238f.web.app` itself. What works offline from the sandbox: generating the Android Studio project skeleton via `@bubblewrap/core`'s `TwaManifest`/`TwaGenerator` directly, and generating the signing keystore with the local JDK's `keytool`. The actual compile has to happen either in Android Studio on a real machine, or — the path actually used — **pwabuilder.com** from the user's own browser, pointed at `https://maxtr-c238f.web.app`.
- **Signing keystore is intentionally not committed** — generated once (`keytool -genkeypair`, RSA/3072, PKCS12) and handed to the account owner as a downloadable file; losing it means never publishing an update under `ua.rytm.app` again. If it ever needs regenerating, `.well-known/assetlinks.json`'s fingerprint must be updated and hosting redeployed to match.
- **PKCS12 keystore gotcha**: `keytool` on JDK 9+ defaults to PKCS12, which does **not** support a separate private-key password — `-keypass X -storepass Y` with `X != Y` silently succeeds and just reuses the store password, printing only a warning. A consumer later told "the key password is X" fails with `javax.crypto.BadPaddingException` (reads like corruption, not a wrong password). **Always generate PKCS12 signing keys with the same value for both `-storepass` and `-keypass`**, and only document/hand off one password.
- **`android-icon-rounded.png`** (repo root, Pillow squircle mask of `icon-512.png`) is pasted into PWABuilder's Android package form "Icon URL" field only — not referenced in `manifest.json`. The TWA splash screen centers the raw icon with no adaptive-icon masking, so the hard square corners looked jarring there specifically; the form's separate "Maskable icon URL" field must keep pointing at the original full-bleed `icon-512.png`.
- **`icon-192-rounded.png`/`icon-512-rounded.png`** (repo root) solve the same problem for desktop-PWA-install (Windows shortcuts): `manifest.json`'s `icons` array has 4 entries — `icon-*-rounded.png` at `"purpose":"any"`, original `icon-*.png` at `"purpose":"maskable"` only (a combined `"any maskable"` purpose was contradictory — "any" wants a pre-rounded source, "maskable" wants full-bleed square). Both rounded files are in `sw.js`'s `STATIC_ASSETS`. `apple-touch-icon`/notification icons deliberately still point at the original square `icon-192.png` (iOS applies its own squircle mask; notification-tray icons are too small for rounding to matter).
- Rebuilding the Android package after a manifest/icon change is manual: pwabuilder.com → `https://maxtr-c238f.web.app` → Package For Stores → Google Play → fill Package ID/Icon URL/Maskable icon URL → Signing key → "Mine" → upload the existing `.jks` + its one password → Download. No CI/automation for this.
- **Google Play Console developer account created, pending Google's own verification review** — nothing can be uploaded until it clears (Google's queue, no action available here). Once cleared: finish the Store listing (screenshots, feature graphic, Data safety form, content rating), upload the already-generated `.aab` to Internal testing, then Production — see `SETUP.md` §9.
