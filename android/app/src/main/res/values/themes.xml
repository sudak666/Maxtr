<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- XML theme only backs the launch window before Compose takes over;
         all real theming (colors/typography) lives in ui/theme/*.kt. -->
    <style name="Theme.Zminka" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/zminka_bg_dark</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/zminka_bg_dark</item>
    </style>
</resources>
