{
  "name": "rytm-functions",
  "description": "Scheduled push-notification sweep for Rytm (budget/recurring/daily reminders while the app is closed).",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
